/target
**/*.rs.bk
Cargo.lock.bak

# local SQLite data
*.db
*.db-journal
*.db-wal
*.db-shm
data/

# env + secrets
.env
.env.*
!.env.example

# editor / OS
.DS_Store
.idea/
.vscode/

# brainstorm visual companion mockups
.superpowers/
.fuxi-build-sha
**/__pycache__/

# 本地 Claude Code 配置 + worktrees / playwright MCP 会话缓存——不入仓
.claude/
.playwright-mcp/

# 讯飞 IVW SDK —— 二进制 .so + 许可证不允许重分发，本地开发用，不入仓
Linux_ivw_*/
deploy/wake/sdk/

