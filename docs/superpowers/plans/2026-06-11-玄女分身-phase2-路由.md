# 玄女分身 Phase 2 · 路由实装计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 用户切 topic 从「kill+spawn+灌回顾」改为「常驻分身秒切 + current_topic 路由」，并修非 general 分身回复不落库的 latent bug、补未读小红点、执行 LRU cap。

**Architecture:** 复用已 ship 的 `XuannvPool` / `TopicXuannvSpawner` / 持久队列；用户消息路由从单值 `xuannv_id()` 改 `ensure_xuannv_for_topic(current_topic_id())`；PWA 可见性路径（conv sync + conv WS）改 pool-aware。`xuannv_id()` 兼容壳保持 = general 镜像（方案甲，spec §3）。

**Tech Stack:** Rust（fuxi-orchestrator / fuxi-cli / fuxi-im）+ sqlx migration + SolidJS PWA。

**Spec:** `docs/superpowers/specs/2026-06-11-玄女分身-phase2-路由-design.md`

**执行模式:** agent team 并行——块A/B/C/D 四个 teammate 各占 worktree 子分支，从母分支
`feat/fuxi-xuannv-phase2-routing` 切出；完成后 merge 回母分支（CLAUDE.md Git 公理 4），
块E 由 team-lead 收口。每块内部严格 TDD：先写测试 → 跑红 → 实现 → 跑绿 → commit。

**门禁（每块完成前必跑）:**
```bash
cargo fmt --all --check
cargo clippy --workspace --all-targets -- -D warnings
cargo test -p <本块改的 crate>
```

---

## 块A · 路由与切换（fuxi-cli + fuxi-im handlers）

**Files:**
- Modify: `crates/fuxi-cli/src/topic_switch.rs`（重写 switch_topic_to，删 load_recent_messages）
- Modify: `crates/fuxi-cli/src/topic_switcher_impl.rs`（瘦身构造参数）
- Modify: `crates/fuxi-cli/src/im.rs:564-573`（CliXuannvSwitcher::new 调用点）
- Modify: `crates/fuxi-orchestrator/src/xuannv_switcher.rs`（trait doc 契约）
- Modify: `crates/fuxi-im/src/handlers/intervene.rs`（缺省 target 路由）
- Modify: `crates/fuxi-im/src/handlers/dispatch.rs:52`（同上 + task 打 topic）
- Modify: `crates/fuxi-im/src/handlers/vision.rs:190-195`（同上）
- **不要碰** `crates/fuxi-im/src/handlers/topics.rs`（块C 独占，避免冲突）
- **不要碰** `crates/fuxi-im/src/conv_store.rs` / `handlers/conv.rs`（块B 独占）

### Task A1: 重写 switch_topic_to

- [ ] **Step 1: 写失败测试**（`topic_switch.rs` tests mod 追加；保留现有 prelude 测试不动）

```rust
// tests mod 顶部追加 imports：
use fuxi_core::id::AgentId;
use fuxi_events::EventBus;
use fuxi_im::db::init_at;
use fuxi_im::topic_store::TopicStore;
use fuxi_orchestrator::Fuxi;
use fuxi_workspace::GitWorktreeWorkspace;
use std::sync::Arc;

async fn make_fuxi() -> Arc<Fuxi> {
    let dir = tempfile::tempdir().unwrap();
    let ws = Arc::new(GitWorktreeWorkspace::with_default_base(
        dir.path().to_path_buf(),
    ));
    let bus = EventBus::with_memory_store().await.unwrap();
    std::mem::forget(dir);
    Arc::new(Fuxi::new(bus, ws))
}

async fn open_topic_store() -> (tempfile::TempDir, TopicStore) {
    let dir = tempfile::tempdir().unwrap();
    let pool = init_at(&dir.path().join("im.db")).await.unwrap();
    (dir, TopicStore::new(pool))
}

#[tokio::test]
async fn switch_hot_path_keeps_clone_and_flips_topic() {
    // Phase 2 核心：池里已有活分身 → 秒切，分身 id 不变（不 kill 不 respawn）。
    let (_dir, topic_store) = open_topic_store().await;
    let fuxi = make_fuxi().await;
    let meta = topic_store.create("画画").await.unwrap();
    let clone = AgentId::new();
    fuxi.set_xuannv_for_topic(meta.id, clone).await;

    switch_topic_to(&fuxi, &topic_store, meta.id)
        .await
        .expect("热路径 switch 应成功");

    assert_eq!(fuxi.current_topic_id(), meta.id);
    assert_eq!(
        fuxi.xuannv_id_for_topic(meta.id).await,
        Some(clone),
        "热路径不得 kill / 换分身"
    );
}

#[tokio::test]
async fn switch_cold_path_without_spawner_bails_and_keeps_topic() {
    // 池 miss + spawner 未注入 → ensure 返 None → bail，current_topic 不动
    //（Phase 1 失败语义保留：失败不 flip）。
    let (_dir, topic_store) = open_topic_store().await;
    let fuxi = make_fuxi().await;
    let meta = topic_store.create("新话题").await.unwrap();
    let before = fuxi.current_topic_id();

    let r = switch_topic_to(&fuxi, &topic_store, meta.id).await;

    assert!(r.is_err(), "ensure 失败应 bail");
    assert_eq!(fuxi.current_topic_id(), before, "失败不得 flip current_topic");
}

#[tokio::test]
async fn switch_rejects_unknown_topic() {
    let (_dir, topic_store) = open_topic_store().await;
    let fuxi = make_fuxi().await;
    let r = switch_topic_to(&fuxi, &topic_store, fuxi_core::TopicId::new()).await;
    assert!(r.is_err(), "不存在的 topic 应拒切");
}
```

注意 `fuxi-cli/Cargo.toml` 的 dev-dependencies 需要 `fuxi-workspace`（若 [dev-dependencies]
没有就加；fuxi-events / fuxi-im / fuxi-orchestrator 主依赖已有）。

- [ ] **Step 2: 跑红**

```bash
cargo test -p fuxi-cli topic_switch
```
预期：编译错（switch_topic_to 签名不符）。

- [ ] **Step 3: 重写实现**

`topic_switch.rs` 整体替换 `switch_topic_to` + 删 `load_recent_messages` +
头注释改写。**保留** `DEFAULT_RECENT_MESSAGES` / `MESSAGE_PREVIEW_CHAR_LIMIT` /
`PRELUDE_TOTAL_CHAR_LIMIT` / `build_topic_prelude` / `display_role` /
`extract_message_text` / `truncate_chars` 及其测试（`xuannv_spawner_impl.rs` 在用）。

```rust
//! Phase 2 · 切 topic 入口。
//!
//! Phase 1（kill 当前 cc → 拉历史拼 prelude → spawn 新 cc）已废：切 topic 不再
//! 杀进程。现在切 = `set_current_topic` + `ensure_xuannv_for_topic`——池里有活
//! 分身就毫秒级秒切（上下文真留内存，不重灌回顾）；没有则懒启动（走
//! [`crate::xuannv_spawner_impl::TopicXuannvSpawner`]：按 topic 过滤的回顾
//! prelude + FUXI_TOPIC env + drain 持久队列）。旧分身留给 idle_gc dormant 回收。
//!
//! 本文件剩下的 prelude 拼装函数（[`build_topic_prelude`] 等）是懒启动路径的
//! 共享件，被 spawner impl 复用——别删。
//!
//! 设计依据：spec `2026-06-11-玄女分身-phase2-路由-design.md` §4.1。

use anyhow::{Context, Result};
use fuxi_core::TopicId;
use fuxi_im::conv_store::Message;
use fuxi_im::topic_store::TopicStore;
use fuxi_orchestrator::Fuxi;
use tracing::{info, warn};

/// 懒启动拉 topic 历史拼 prelude 时的最近消息条数。
pub const DEFAULT_RECENT_MESSAGES: usize = 50;
/// 单条消息文本预览截断字符上限——避免单条爆炸长把 prelude 撑爆。
pub const MESSAGE_PREVIEW_CHAR_LIMIT: usize = 240;
/// prelude 文本总字符上限（粗保护，超出按整段截尾）。handoff §3.2 给的指标：≤ 1500 字。
pub const PRELUDE_TOTAL_CHAR_LIMIT: usize = 1500;

/// 切 topic 到 `target_id`——Phase 2 路径：验证 topic 存在 → ensure 分身
/// （热路径 = 池查询毫秒级；冷路径 = spawner 懒启动数秒）→ commit
/// `set_current_topic` → touch_last_active。
///
/// 失败语义：ensure 失败（spawner 未注入 / spawn 炸）整体 bail，
/// current_topic **不** flip——调用方按需 retry（HTTP 5xx）。
/// 不杀旧分身、不等旧分身 idle：旧 turn 跑完输出落它自己的 topic。
pub async fn switch_topic_to(
    fuxi: &Fuxi,
    topic_store: &TopicStore,
    target_id: TopicId,
) -> Result<()> {
    // 验证 topic 存在；不存在拒切（避免 typo 导致玄女绑到孤儿 id）。
    let target_meta = topic_store
        .get(target_id)
        .await
        .with_context(|| format!("查 topic {target_id} 失败"))?
        .with_context(|| format!("topic {target_id} 不存在，先创建"))?;

    let clone = fuxi
        .ensure_xuannv_for_topic(target_id)
        .await
        .context("ensure 分身失败（spawner 未注入或 spawn 失败）")?;

    fuxi.set_current_topic(target_id).await;
    if let Err(err) = topic_store.touch_last_active(target_id).await {
        warn!(?err, "touch_last_active 失败，sidebar 排序可能滞后");
    }
    info!(
        %clone,
        target = %target_id,
        title = %target_meta.title,
        "topic 切换完成（常驻分身，未 kill）"
    );
    Ok(())
}
```

`build_topic_prelude` 的导语文案把「你刚被 fuxi 切到」保持原样（懒启动首次 spawn
仍要回顾）。删掉文件里所有 `ConvStore` / `OracleStore` / `spawn_with_prelude` /
`wait_xuannv_idle` / `SCOPE_XUANNV` 的 import 与使用。

- [ ] **Step 4: 跑绿**

```bash
cargo test -p fuxi-cli topic_switch
```
预期：新 3 测 + 原 prelude 测试全 PASS。

- [ ] **Step 5: Commit**

```bash
git add crates/fuxi-cli/src/topic_switch.rs crates/fuxi-cli/Cargo.toml
git commit -m "feat(topic): Phase 2 切 topic 改常驻分身秒切——不 kill 不重灌回顾"
```

### Task A2: CliXuannvSwitcher 瘦身 + trait 契约更新

- [ ] **Step 1: 改 `topic_switcher_impl.rs`**——字段只留 `fuxi` + `topic_store`：

```rust
pub struct CliXuannvSwitcher {
    fuxi: Arc<Fuxi>,
    topic_store: TopicStore,
}

impl CliXuannvSwitcher {
    pub fn new(fuxi: Arc<Fuxi>, topic_store: TopicStore) -> Self {
        Self { fuxi, topic_store }
    }
}

#[async_trait]
impl XuannvSwitcher for CliXuannvSwitcher {
    async fn switch_topic(&self, topic_id: TopicId) -> Result<()> {
        crate::topic_switch::switch_topic_to(self.fuxi.as_ref(), &self.topic_store, topic_id)
            .await
            .map_err(|e| OrchestratorError::Other(format!("switch_topic_to: {e}")))
    }
}
```

删多余 import（OracleStore / ConvStore）；头注释「焊起来」段补一句
「Phase 2：切换不再 spawn，oracle/conv_store/role 移交 TopicXuannvSpawner」。

- [ ] **Step 2: 改 `im.rs:564-573` 调用点**

```rust
fuxi.set_xuannv_switcher(Arc::new(
    crate::topic_switcher_impl::CliXuannvSwitcher::new(
        fuxi.clone(),
        topic_store.clone(),
    ),
))
.await;
```

- [ ] **Step 3: 改 `xuannv_switcher.rs` trait doc**——调用契约整段替换：

```rust
/// 调用契约（Phase 2）：调用方传 `topic_id`，impl 负责：
/// 1. 验证 topic 存在（不存在拒切）
/// 2. `ensure_xuannv_for_topic(topic_id)`——池有活分身秒切；无则懒启动
///    （topic 过滤回顾 prelude + drain 持久队列）
/// 3. `set_current_topic(topic_id)` + `touch_last_active(topic_id)`
///
/// **不** kill 旧分身（留 idle_gc dormant）、**不**等旧分身 idle。
///
/// 失败语义：ensure 失败应 bail 且不 flip current_topic——调用方按需
/// retry（HTTP 5xx）。
```

- [ ] **Step 4: 编译 + 跑测**

```bash
cargo check -p fuxi-cli -p fuxi-orchestrator
cargo test -p fuxi-cli
```

- [ ] **Step 5: Commit**

```bash
git add crates/fuxi-cli/src/topic_switcher_impl.rs crates/fuxi-cli/src/im.rs crates/fuxi-orchestrator/src/xuannv_switcher.rs
git commit -m "refactor(topic): XuannvSwitcher 契约改 Phase 2——switcher 不再持 spawn 链路依赖"
```

### Task A3: intervene 缺省 target 改 current_topic 路由

- [ ] **Step 1: 写失败测试**（`intervene.rs` tests mod 追加；现有测试不动——
  `set_xuannv` 绑 general + current_topic 默认 general，行为兼容）

```rust
#[tokio::test]
async fn no_target_routes_to_current_topic_clone() {
    // Phase 2 路由回归：current_topic = B 且池里有 B 分身时，缺省 target 必须
    // 路由到 B 分身（而非 general 镜像）。B 分身不在 shelf → Fuxi::intervene
    // 撞 AgentNotFound → 503，但 message 是 orchestrator 错——证明 fallback
    // 已找到分身并继续走了 intervene（若仍走旧 xuannv_id() 路径，general 镜像
    // 为 None，会 503 "玄女尚未就绪"，在 publish 之前就断）。
    let (_dir, app, fuxi) = build_app().await; // 本文件现有 helper（返 Router + Arc<Fuxi>）
    let topic = fuxi_core::TopicId::new();
    let clone = AgentId::new();
    fuxi.set_xuannv_for_topic(topic, clone).await;
    fuxi.set_current_topic(topic).await;

    let resp = app
        .oneshot(
            Request::builder()
                .method("POST")
                .uri("/api/intervene")
                .header("content-type", "application/json")
                .body(Body::from(r#"{"text":"你好"}"#))
                .unwrap(),
        )
        .await
        .unwrap();

    assert_eq!(resp.status(), StatusCode::SERVICE_UNAVAILABLE);
    let body = to_bytes(resp.into_body(), usize::MAX).await.unwrap();
    let text = String::from_utf8_lossy(&body);
    assert!(
        !text.contains("玄女尚未就绪"),
        "不应再走旧 xuannv_id() 路径报『尚未就绪』，实际: {text}"
    );
}
```

（`build_app` 是该文件 tests mod 现有 helper，签名
`async fn build_app() -> (tempfile::TempDir, Router, Arc<Fuxi>)`；
若其内部已把 `/api/intervene` 挂上 Router 就直接 oneshot，否则照其余测试用法。）

- [ ] **Step 2: 跑红**

```bash
cargo test -p fuxi-im intervene::tests::no_target_routes_to_current_topic_clone
```
预期：FAIL（body 含「玄女尚未就绪」——旧路径 general 镜像 None）。

- [ ] **Step 3: 改实现**（`intervene.rs:116-146` + `:201-202` 区域）

```rust
    // Phase 2 路由：缺省 target / dead-target fallback 都落「当前 topic 的分身」。
    // ensure：池 miss 时懒启动（热路径只是池查询 + LRU touch，毫秒级）。
    let current_topic = state.fuxi.current_topic_id();
    let mut target = match body
        .target
        .as_deref()
        .map(str::trim)
        .filter(|s| !s.is_empty())
    {
        Some(t) => parse_agent_id_lenient(t)
            .map_err(|e| Error::BadRequest(format!("target 解析失败: {e}")))?,
        None => state
            .fuxi
            .ensure_xuannv_for_topic(current_topic)
            .await
            .ok_or_else(|| {
                Error::Unavailable("玄女尚未就绪——请稍后重试或检查 daemon 启动".into())
            })?,
    };

    // 「target 是玄女」的判定从单值比对改池成员判定——任一活分身都是玄女。
    let pool = state.fuxi.xuannv_pool();
    let target_is_xuannv_pre = pool.is_active_clone(target).await;
    let mut dead_target_hint: Option<String> = None;
    if !target_is_xuannv_pre && state.fuxi.status_of(target).await.is_none() {
        let xn = state
            .fuxi
            .ensure_xuannv_for_topic(current_topic)
            .await
            .ok_or_else(|| {
                Error::Unavailable("玄女尚未就绪——目标门客已下线，无法 fallback".into())
            })?;
        dead_target_hint = Some(format!(
            "[用户原本 @ 了门客 {target}，但该门客已下线（可走 `fuxi spawn --recall-task <id>` 召回 session）。请你接续答复用户。]"
        ));
        target = xn;
    }
```

下方 `:201-202` 的

```rust
    let xuannv_id = xuannv_for_fallback;
    let target_is_xuannv = xuannv_id == Some(target);
```

替换为

```rust
    let target_is_xuannv = pool.is_active_clone(target).await;
```

（`xuannv_for_fallback` / `target_is_xuannv_pre` 旧变量随之清干净；
文件头 `//! 1. 取 state.fuxi.xuannv_id()` 的流程注释同步改。）

- [ ] **Step 4: 跑绿（全文件）**

```bash
cargo test -p fuxi-im intervene
```
预期：新测 + 旧测全 PASS。

- [ ] **Step 5: Commit**

```bash
git add crates/fuxi-im/src/handlers/intervene.rs
git commit -m "feat(im): intervene 缺省 target 改 current_topic 分身路由（Phase 2）"
```

### Task A4: dispatch + vision 同款路由

- [ ] **Step 1: `dispatch.rs:52` 改**

```rust
    let topic = state.fuxi.current_topic_id();
    let xuannv = state
        .fuxi
        .ensure_xuannv_for_topic(topic)
        .await
        .ok_or_else(|| {
            Error::Unavailable("玄女尚未就绪——请稍后重试或检查 daemon 启动".into())
        })?;

    let mut task = Task::new(title, description);
    // Phase 2：root task 打 current topic——门客事件经 bridge 归位本 topic 分身。
    task = task.with_topic_id(topic);
```

（原 `let mut task = Task::new(...)` 行合并进来，project_id 分支保持不动；
文件头 `//! 1. 取 state.fuxi.xuannv_id()` 注释同步改。）

- [ ] **Step 2: `vision.rs:190-195` 改**

```rust
    // 玄女未起 → 503。Phase 2：look 注入「当前 topic 的分身」——conv WS 按
    // current_topic 过滤（块B），打错分身桌宠收不到帧请求。
    let xuannv = state
        .fuxi
        .ensure_xuannv_for_topic(state.fuxi.current_topic_id())
        .await
        .ok_or_else(|| Error::Unavailable("玄女尚未注入；请先 set_xuannv".into()))?;
```

- [ ] **Step 3: 跑测 + 门禁**

```bash
cargo test -p fuxi-im
cargo fmt --all --check && cargo clippy -p fuxi-im -p fuxi-cli -p fuxi-orchestrator --all-targets -- -D warnings
```
预期：全绿（dispatch/vision 现有测试用 set_xuannv → general 池 + current=general，兼容）。

- [ ] **Step 4: Commit**

```bash
git add crates/fuxi-im/src/handlers/dispatch.rs crates/fuxi-im/src/handlers/vision.rs
git commit -m "feat(im): dispatch/vision 玄女入口改 current_topic 分身路由"
```

---

## 块B · PWA 可见性 pool-aware（fuxi-im conv 路径）

**Files:**
- Modify: `crates/fuxi-im/src/conv_store.rs`（spawn_xuannv_sync + handle_event）
- Modify: `crates/fuxi-im/src/handlers/conv.rs`（conv_ws filter）
- Modify: `crates/fuxi-cli/src/im.rs:542-548`（sync 装配传 pool watch）
- **不要碰** `handlers/intervene.rs` / `dispatch.rs` / `vision.rs` / `topics.rs`（块A/C 独占）

### Task B1: handle_event 改池成员过滤 + topic 归属

- [ ] **Step 1: 写失败测试**（`conv_store.rs` tests mod 追加）

```rust
use fuxi_core::TopicId;
use fuxi_core::id::AgentId;
use std::collections::HashMap;

fn ev_responded(agent: AgentId, text: &str, topic: Option<TopicId>) -> Event {
    let mut meta = EventMeta::now();
    meta.agent = Some(agent);
    if let Some(t) = topic {
        meta.topic_id = Some(t);
    }
    Event {
        meta,
        kind: EventKind::AgentResponded {
            text: text.to_string(),
            artifact_ref: None,
        },
    }
}

#[tokio::test]
async fn clone_response_lands_in_its_topic_not_current() {
    // latent-bug 核心回归：topic B 分身回话（meta.topic_id 缺省），current=general
    // → 必须落库且归 B（旧实现：单值过滤直接 drop，PWA 历史丢失）。
    let (_dir, store) = open_store().await;
    let conv = store.ensure_scope(SCOPE_XUANNV, None).await.unwrap();
    let topic_b = TopicId::new();
    let clone_b = AgentId::new();
    let pool: HashMap<TopicId, AgentId> = [(topic_b, clone_b)].into();

    handle_event(
        &store,
        &conv,
        &pool,
        TopicId::general(),
        &ev_responded(clone_b, "门客干完了", None),
    )
    .await
    .unwrap();

    let (msgs, _, _) = store
        .page_messages_in_topic(&conv, &topic_b.0.to_string(), 10, None)
        .await
        .unwrap();
    assert_eq!(msgs.len(), 1, "B 分身的回复必须落 B 的历史");
    assert_eq!(msgs[0].role, "xuannv");
    let (general_msgs, _, _) = store
        .page_messages_in_topic(&conv, &TopicId::general().0.to_string(), 10, None)
        .await
        .unwrap();
    assert!(general_msgs.is_empty(), "不得串进 current topic");
}

#[tokio::test]
async fn non_clone_agent_response_not_persisted() {
    // 门客（非分身）的 AgentResponded 不入玄女主线——保持旧契约。
    let (_dir, store) = open_store().await;
    let conv = store.ensure_scope(SCOPE_XUANNV, None).await.unwrap();
    let pool: HashMap<TopicId, AgentId> = [(TopicId::general(), AgentId::new())].into();

    handle_event(
        &store,
        &conv,
        &pool,
        TopicId::general(),
        &ev_responded(AgentId::new(), "我是门客", None),
    )
    .await
    .unwrap();

    let (msgs, _, _) = store
        .page_messages_in_topic(&conv, &TopicId::general().0.to_string(), 10, None)
        .await
        .unwrap();
    assert!(msgs.is_empty());
}

#[tokio::test]
async fn meta_topic_id_takes_precedence_over_pool_slot() {
    // 事件已带 meta.topic_id（门客适配器 stamp 过）→ 以它为准，不看池槽位。
    let (_dir, store) = open_store().await;
    let conv = store.ensure_scope(SCOPE_XUANNV, None).await.unwrap();
    let topic_b = TopicId::new();
    let topic_c = TopicId::new();
    let clone_b = AgentId::new();
    let pool: HashMap<TopicId, AgentId> = [(topic_b, clone_b)].into();

    handle_event(
        &store,
        &conv,
        &pool,
        TopicId::general(),
        &ev_responded(clone_b, "带 stamp 的", Some(topic_c)),
    )
    .await
    .unwrap();

    let (msgs, _, _) = store
        .page_messages_in_topic(&conv, &topic_c.0.to_string(), 10, None)
        .await
        .unwrap();
    assert_eq!(msgs.len(), 1, "meta.topic_id 优先于池槽位反查");
}
```

- [ ] **Step 2: 跑红**

```bash
cargo test -p fuxi-im conv_store
```
预期：编译错（handle_event 签名不符）。

- [ ] **Step 3: 改实现**

helper（`handle_event` 上方）：

```rust
/// 池槽位反查：agent 是哪个 topic 的活分身。非分身返 None。
fn clone_topic_of(pool: &HashMap<TopicId, AgentId>, id: AgentId) -> Option<TopicId> {
    pool.iter().find_map(|(t, a)| (*a == id).then_some(*t))
}

/// 消息 topic 归属：事件自带 stamp 优先（门客适配器写的）→ 池槽位反查（分身
/// 自己的事件不一定带 stamp）→ current_topic 兜底（分身刚 dormant 的 in-flight
/// 残留事件，按旧行为落当前 topic，不丢消息）。
fn attribute_topic(
    pool: &HashMap<TopicId, AgentId>,
    current: TopicId,
    ev: &Event,
    subject: AgentId,
) -> TopicId {
    ev.meta
        .topic_id
        .or_else(|| clone_topic_of(pool, subject))
        .unwrap_or(current)
}
```

`handle_event` 签名改：

```rust
async fn handle_event(
    store: &ConvStore,
    conv_id: &str,
    pool: &HashMap<TopicId, AgentId>,
    current_topic: TopicId,
    ev: &Event,
) -> Result<()> {
    let source_id = ev.meta.id.to_string();
```

五个 match arm 的 guard 与 topic 逐一改（`topic_str` 不再函数头算一次，
每 arm 按 subject 算）：

- `UserPrompted`：guard `if ev.meta.agent.is_some_and(|a| clone_topic_of(pool, a).is_some())`；
  arm 内 `let subject = ev.meta.agent.expect("guard 保证");
  let topic_str = attribute_topic(pool, current_topic, ev, subject).0.to_string();`
- `UserInterventionSent`：guard `if clone_topic_of(pool, *target).is_some()`；
  subject = `*target`。
- `AgentResponded`：guard 同 UserPrompted；`agent_id` 字段写真实分身 id：
  `Some(&subject.to_string())`。
- `TaskCreated`：guard 同 UserPrompted；agent_id 同上改 subject。
- `OrchestratorCcReceived`：guard 同 UserPrompted。

`spawn_xuannv_sync` 签名与 loop 改：

```rust
pub async fn spawn_xuannv_sync(
    store: Arc<ConvStore>,
    bus: EventBus,
    pool_watch: watch::Receiver<HashMap<TopicId, AgentId>>,
    current_topic_watch: watch::Receiver<TopicId>,
) -> tokio::task::JoinHandle<()> {
```

loop 内原 `let Some(current_xn) = ... else { continue }` 替换为：

```rust
            // 每条事件 borrow 当前池快照——respawn / dormant 漂移实时跟随
            //（克隆开销 = cap 个 entry，可忽略）。空池 = 玄女尚未上线，兜底防
            // daemon 启动顺序 race。
            let pool_snapshot = pool_watch.borrow().clone();
            if pool_snapshot.is_empty() {
                continue;
            }
            let topic = *current_topic_watch.borrow();
            if let Err(e) = handle_event(&store, &conv_id, &pool_snapshot, topic, &ev).await {
                warn!(error = %e, kind = ?ev.kind, "conv_store sync 写库失败");
            }
```

doc 注释同步改（「xuannv_id_watch」段落改成池 watch 语义，引用
memory `feedback_dynamic_agent_id_via_watch` 那段保留精神）。
顶部 `use` 加 `std::collections::HashMap`、`fuxi_core::TopicId`（已有则略）。

本文件旧测试若直接调 `handle_event(store, conv, xuannv_id, topic, ev)`，
统一改为 `handle_event(store, conv, &[(topic, xuannv_id)].into(), topic, ev)`——
单分身池 = 旧语义，断言不变。调 `spawn_xuannv_sync` 的测试把
`watch::channel(Some(id))` 改 `watch::channel(HashMap::from([(TopicId::general(), id)]))`。

- [ ] **Step 4: 跑绿**

```bash
cargo test -p fuxi-im conv_store
```

- [ ] **Step 5: Commit**

```bash
git add crates/fuxi-im/src/conv_store.rs
git commit -m "fix(im): conv sync 改 pool-aware——修非 general 分身回复不落库 latent bug"
```

### Task B2: conv_ws 按 current_topic 过滤

- [ ] **Step 1: 写失败测试**（`conv.rs` tests mod 追加——免起真 socket，
  抽 filter 自由函数单测，仿 `workers.rs::worker_event_visible` pattern）

```rust
#[test]
fn conv_visible_only_current_topic_clone_events() {
    use fuxi_core::TopicId;
    use fuxi_core::event::{EventKind, EventMeta};
    use fuxi_core::id::AgentId;
    use std::collections::HashMap;

    let topic_a = TopicId::new();
    let topic_b = TopicId::new();
    let clone_a = AgentId::new();
    let clone_b = AgentId::new();
    let pool: HashMap<TopicId, AgentId> = [(topic_a, clone_a), (topic_b, clone_b)].into();

    let ev = |agent: Option<AgentId>| {
        let mut meta = EventMeta::now();
        meta.agent = agent;
        fuxi_core::event::Event {
            meta,
            kind: EventKind::AgentResponded {
                text: "hi".into(),
                artifact_ref: None,
            },
        }
    };

    // 当前 topic = A：A 分身可见，B 分身不可见（落库走小红点），门客/平台级不可见
    assert!(conv_event_visible(&pool, topic_a, &ev(Some(clone_a))));
    assert!(!conv_event_visible(&pool, topic_a, &ev(Some(clone_b))));
    assert!(!conv_event_visible(&pool, topic_a, &ev(Some(AgentId::new()))));
    assert!(!conv_event_visible(&pool, topic_a, &ev(None)));
    // 切到 B 后跟随
    assert!(conv_event_visible(&pool, topic_b, &ev(Some(clone_b))));
}
```

- [ ] **Step 2: 跑红**

```bash
cargo test -p fuxi-im conv::tests::conv_visible_only_current_topic_clone_events
```
预期：编译错（conv_event_visible 不存在）。

- [ ] **Step 3: 实现**（`conv.rs`，`conv_ws` 上方加函数 + handler 改 filter）

```rust
/// 玄女对话流过滤（Phase 2）：只放行「池中活分身」的事件，且分身归属 topic ==
/// 当前 topic。归属用池槽位反查——分身自己的事件不一定带 meta.topic_id，池是
/// 「谁服务哪个 topic」的唯一真相源。B 分身的主动汇报被本过滤拦下，走
/// 落库 + 未读小红点 + push 三件套，不插进 A 的实时视图。
pub(crate) fn conv_event_visible(
    pool: &std::collections::HashMap<fuxi_core::TopicId, fuxi_core::id::AgentId>,
    current: fuxi_core::TopicId,
    ev: &fuxi_core::event::Event,
) -> bool {
    let Some(agent) = ev.meta.agent else {
        return false;
    };
    let Some(topic) = pool.iter().find_map(|(t, a)| (*a == agent).then_some(*t)) else {
        return false;
    };
    topic == current
}
```

`conv_ws` 内：

```rust
    // Phase 2：filter 改池 watch + current_topic watch 双实时——分身 respawn 漂移、
    // 用户切 topic 都即时跟随（绝不 snapshot，见 memory
    // `feedback_dynamic_agent_id_via_watch`）。503 判断保留 general 镜像：general
    // 永驻（idle_gc 豁免），镜像 None = 玄女体系尚未 boot。
    let pool_watch = state.fuxi.xuannv_pool_watch();
    let topic_watch = state.fuxi.current_topic_watch();
    let id_watch = state.fuxi.xuannv_id_watch();
    let xuannv_now = *id_watch.borrow();
    if xuannv_now.is_none() {
        return Err(Error::NotFound("玄女尚未注入；请先 set_xuannv".into()));
    }
    info!(?cursor, xuannv = ?xuannv_now, "ws /api/conv accept");

    let bus = state.fuxi.bus().clone();
    let stream = build_event_stream(&bus, cursor);

    let resp = ws.on_upgrade(move |socket| async move {
        run_ws_loop(socket, stream, move |ev| {
            conv_event_visible(&pool_watch.borrow(), *topic_watch.borrow(), ev)
        })
        .await;
    });
    Ok(resp)
```

文件头 `//! 仅推 meta.agent == xuannv_id` 注释段同步改写。

- [ ] **Step 4: 跑绿 + 全 crate**

```bash
cargo test -p fuxi-im
```
注意 `tests/ws_stream.rs` e2e 若按旧单值语义断言，需把场景改成
「set_xuannv(general 分身) + current=general」——单分身即旧行为，断言应保持可过；
真挂了按新语义修断言而非削功能。

- [ ] **Step 5: Commit**

```bash
git add crates/fuxi-im/src/handlers/conv.rs
git commit -m "feat(im): conv WS 改 current_topic 归属过滤——B 分身汇报不插 A 视图"
```

### Task B3: im.rs 装配换 pool watch

- [ ] **Step 1: `im.rs:542-548` 改**

```rust
                let h = fuxi_im::conv_store::spawn_xuannv_sync(
                    Arc::new(conv_store.clone()),
                    bus.clone(),
                    fuxi.xuannv_pool_watch(),
                    fuxi.current_topic_watch(),
                )
                .await;
```

（上方那段「传 watch 而不是静态 id」注释改成池版本措辞。）

- [ ] **Step 2: 门禁**

```bash
cargo check -p fuxi-cli
cargo fmt --all --check && cargo clippy -p fuxi-im -p fuxi-cli --all-targets -- -D warnings && cargo test -p fuxi-im
```

- [ ] **Step 3: Commit**

```bash
git add crates/fuxi-cli/src/im.rs
git commit -m "chore(im): conv sync 装配改传分身池 watch"
```

---

## 块C · 未读小红点（fuxi-im + web）

**Files:**
- Create: `crates/fuxi-im/migrations/0010_topic_read_watermarks.sql`
- Modify: `crates/fuxi-im/src/topic_store.rs`（mark_read + unread_counts）
- Modify: `crates/fuxi-im/src/handlers/topics.rs`（TopicView.unread_count + read 端点 + doc 修正）
- Modify: `crates/fuxi-im/src/router.rs`（read 路由）
- Modify: `crates/fuxi-im/web/src/types/api.ts` / `web/src/lib/api.ts` /
  `web/src/components/TopicSidebar.tsx` / `web/src/components/TopicSidebar.module.css` /
  `web/src/views/pages/XuannvPage.tsx`（overlay 文案）/ `web/tests/mocks/api.ts`
- **不要碰** intervene/dispatch/vision/conv_store/conv.rs（块A/B 独占）

### Task C1: migration + TopicStore 水位

- [ ] **Step 1: 建 migration**

`crates/fuxi-im/migrations/0010_topic_read_watermarks.sql`：

```sql
-- Phase 2 · topic 未读水位（spec 2026-06-11-玄女分身-phase2-路由-design.md §4.5）
--
-- 读状态与 topic meta 正交，独立表不 ALTER topics：将来多端/多用户水位扩展
-- 不动 topics 行。无水位行 = 视为全读（unread=0）——避免升级首日 general
-- 的全量历史被计成四位数未读。
CREATE TABLE IF NOT EXISTS topic_read_watermarks (
    topic_id     TEXT PRIMARY KEY,
    last_read_at TEXT NOT NULL          -- RFC3339，与 messages.ts 同口径比较
);
```

- [ ] **Step 2: 写失败测试**（`topic_store.rs` tests mod 追加）

```rust
    use crate::conv_store::{ConvStore, SCOPE_XUANNV};

    /// 往 topic 灌一条消息（role 可指定）——unread 口径测试用。
    async fn seed_msg(pool: &sqlx::SqlitePool, topic: TopicId, role: &str) {
        let conv = ConvStore::new(pool.clone());
        let conv_id = conv.ensure_scope(SCOPE_XUANNV, None).await.unwrap();
        conv.append_message_in_topic(
            &conv_id,
            role,
            None,
            "text",
            &serde_json::json!({"text": "msg"}),
            None,
            None,
            chrono::Utc::now(),
            &topic.0.to_string(),
        )
        .await
        .unwrap();
    }

    #[tokio::test]
    async fn no_watermark_means_zero_unread() {
        let (_dir, store) = open_store().await;
        let m = store.create("新话题").await.unwrap();
        seed_msg(&store.pool, m.id, "xuannv").await;
        let counts = store.unread_counts().await.unwrap();
        assert_eq!(counts.get(&m.id.0.to_string()), None, "无水位 = 全读");
    }

    #[tokio::test]
    async fn unread_counts_after_watermark_excludes_user_messages() {
        let (_dir, store) = open_store().await;
        let m = store.create("画画").await.unwrap();
        store.mark_read(m.id).await.unwrap();
        tokio::time::sleep(std::time::Duration::from_millis(5)).await;
        seed_msg(&store.pool, m.id, "xuannv").await;
        seed_msg(&store.pool, m.id, "user").await; // 自己说的话不算未读
        let counts = store.unread_counts().await.unwrap();
        assert_eq!(counts.get(&m.id.0.to_string()).copied(), Some(1));
    }

    #[tokio::test]
    async fn mark_read_clears_unread() {
        let (_dir, store) = open_store().await;
        let m = store.create("画画").await.unwrap();
        store.mark_read(m.id).await.unwrap();
        tokio::time::sleep(std::time::Duration::from_millis(5)).await;
        seed_msg(&store.pool, m.id, "xuannv").await;
        tokio::time::sleep(std::time::Duration::from_millis(5)).await;
        store.mark_read(m.id).await.unwrap();
        let counts = store.unread_counts().await.unwrap();
        assert_eq!(counts.get(&m.id.0.to_string()), None, "read 后清零");
    }
```

（`store.pool` 是私有字段——同文件 tests 子模块可直接访问。）

- [ ] **Step 3: 跑红**

```bash
cargo test -p fuxi-im topic_store
```

- [ ] **Step 4: 实现**（`topic_store.rs` impl 块追加）

```rust
    /// 把 topic 的未读水位推到 now——PWA 进入/离开话题时调（POST /api/topics/:id/read）。
    pub async fn mark_read(&self, id: TopicId) -> Result<()> {
        sqlx::query(
            "INSERT INTO topic_read_watermarks (topic_id, last_read_at) VALUES (?1, ?2) \
             ON CONFLICT(topic_id) DO UPDATE SET last_read_at = excluded.last_read_at",
        )
        .bind(id.0.to_string())
        .bind(Utc::now().to_rfc3339())
        .execute(&self.pool)
        .await
        .map_err(|e| Error::Internal(format!("topic mark_read: {e}")))?;
        Ok(())
    }

    /// 每 topic 未读数：水位之后的非 user 消息（自己说的话不算未读）。
    /// 无水位行的 topic 不出现在结果里（= 0，视为全读）。
    /// ts 与水位同为 RFC3339 字符串比较——与 page_messages 的排序口径一致。
    pub async fn unread_counts(&self) -> Result<std::collections::HashMap<String, i64>> {
        let rows = sqlx::query(
            "SELECT m.topic_id AS topic_id, COUNT(*) AS n \
             FROM messages m \
             JOIN topic_read_watermarks w ON w.topic_id = m.topic_id \
             WHERE m.role != 'user' AND m.ts > w.last_read_at \
             GROUP BY m.topic_id",
        )
        .fetch_all(&self.pool)
        .await
        .map_err(|e| Error::Internal(format!("topic unread_counts: {e}")))?;
        rows.into_iter()
            .map(|row| {
                let topic_id: String = row
                    .try_get("topic_id")
                    .map_err(|e| Error::Internal(format!("unread row topic_id: {e}")))?;
                let n: i64 = row
                    .try_get("n")
                    .map_err(|e| Error::Internal(format!("unread row n: {e}")))?;
                Ok((topic_id, n))
            })
            .collect()
    }
```

- [ ] **Step 5: 跑绿 + Commit**

```bash
cargo test -p fuxi-im topic_store
git add crates/fuxi-im/migrations/0010_topic_read_watermarks.sql crates/fuxi-im/src/topic_store.rs
git commit -m "feat(im): topic 未读水位——mark_read + unread_counts（migration 0010）"
```

### Task C2: handlers + 路由

- [ ] **Step 1: 写失败测试**（`topics.rs` tests mod 追加。现有 helper
  `build_router() -> (TempDir, Router)` 不外露 store——加一个返 store 的变体，
  原 helper 改调它，老测试零改动）

```rust
    /// build_router 的扩展版：TopicStore + 同 pool 的 ConvStore 一并返出来，
    /// 未读水位测试要直接灌消息库（TopicStore.pool 是私有字段，跨模块拿不到）。
    async fn build_router_with_store()
    -> (tempfile::TempDir, Router, TopicStore, crate::conv_store::ConvStore) {
        let dir = tempfile::tempdir().expect("tmp");
        let pool = db::init_at(&dir.path().join("im.db")).await.expect("init");
        let store = TopicStore::new(pool.clone());
        let conv = crate::conv_store::ConvStore::new(pool);
        let bus = EventBus::with_memory_store().await.unwrap();
        let ws = Arc::new(GitWorktreeWorkspace::with_default_base(dir.path()));
        let fuxi = Arc::new(Fuxi::new(bus, ws));
        let state = AppState::new(fuxi).with_topic_store(store.clone());
        let app = Router::new()
            .route("/api/topics", get(list_topics).post(create_topic))
            .route("/api/topics/current", get(current_topic))
            .route("/api/topics/{id}/archive", post(archive_topic))
            .route("/api/topics/{id}/read", post(mark_topic_read))
            .with_state(state);
        (dir, app, store, conv)
    }

    /// 往 topic 灌一条 xuannv 消息——未读计数口径用（同 topic_store.rs C1 的 seed）。
    async fn seed_xuannv_msg(conv: &crate::conv_store::ConvStore, topic: TopicId) {
        let conv_id = conv
            .ensure_scope(crate::conv_store::SCOPE_XUANNV, None)
            .await
            .unwrap();
        conv.append_message_in_topic(
            &conv_id,
            "xuannv",
            None,
            "text",
            &serde_json::json!({"text": "完工汇报"}),
            None,
            None,
            chrono::Utc::now(),
            &topic.0.to_string(),
        )
        .await
        .unwrap();
    }

    #[tokio::test]
    async fn list_topics_carries_unread_and_forces_current_zero() {
        // current topic 强制 0（用户正看着，WS 实时在喂）；其他 topic 按水位计数。
        let (_dir, app, store, conv) = build_router_with_store().await;
        let m = store.create("别的话题").await.unwrap();
        store.mark_read(m.id).await.unwrap();
        tokio::time::sleep(std::time::Duration::from_millis(5)).await;
        seed_xuannv_msg(&conv, m.id).await;

        let resp = app
            .oneshot(
                Request::builder()
                    .uri("/api/topics")
                    .body(Body::empty())
                    .unwrap(),
            )
            .await
            .unwrap();
        assert_eq!(resp.status(), StatusCode::OK);
        let bytes = resp.into_body().collect().await.unwrap().to_bytes();
        let parsed: TopicsResponse = serde_json::from_slice(&bytes).unwrap();
        let view = parsed
            .topics
            .iter()
            .find(|t| t.id == m.id.0.to_string())
            .unwrap();
        assert_eq!(view.unread_count, 1, "非 current topic 按水位计未读");
        let general = parsed
            .topics
            .iter()
            .find(|t| t.id == TopicId::general().0.to_string())
            .unwrap();
        assert_eq!(general.unread_count, 0, "current topic 强制 0");
    }

    #[tokio::test]
    async fn mark_topic_read_endpoint_returns_204_and_clears() {
        let (_dir, app, store, conv) = build_router_with_store().await;
        let m = store.create("读我").await.unwrap();
        store.mark_read(m.id).await.unwrap();
        tokio::time::sleep(std::time::Duration::from_millis(5)).await;
        seed_xuannv_msg(&conv, m.id).await;

        let resp = app
            .oneshot(
                Request::builder()
                    .method("POST")
                    .uri(format!("/api/topics/{}/read", m.id.0))
                    .body(Body::empty())
                    .unwrap(),
            )
            .await
            .unwrap();
        assert_eq!(resp.status(), StatusCode::NO_CONTENT);
        let counts = store.unread_counts().await.unwrap();
        assert_eq!(counts.get(&m.id.0.to_string()), None, "read 后未读清零");
    }
```

- [ ] **Step 2: 跑红**

```bash
cargo test -p fuxi-im topics
```

- [ ] **Step 3: 实现**

`TopicView` 加字段（+ `From<TopicMeta>` 填 0）：

```rust
    /// Phase 2 · 未读数（水位之后的非 user 消息）。current topic 恒 0。
    #[serde(default)]
    pub unread_count: i64,
```

`list_topics` 改：

```rust
    let unread = store.unread_counts().await.unwrap_or_else(|e| {
        tracing::warn!(error = %e, "unread_counts 失败——badge 退化为 0，不挡列表");
        Default::default()
    });
    let current_topic_id = state.fuxi.current_topic_id().0.to_string();
    let topics = items
        .into_iter()
        .map(|m| {
            let mut v = TopicView::from(m);
            if v.id != current_topic_id {
                v.unread_count = unread.get(&v.id).copied().unwrap_or(0);
            }
            v
        })
        .collect();
    Ok(Json(TopicsResponse {
        topics,
        current_topic_id,
    }))
```

新 handler：

```rust
/// `POST /api/topics/:id/read` —— 推进未读水位（PWA 进入/离开话题时调）。
pub async fn mark_topic_read(
    State(state): State<AppState>,
    AxumPath(id): AxumPath<String>,
) -> Result<StatusCode> {
    let topic_id = parse_topic_id(&id)?;
    let store = state
        .topic_store
        .as_ref()
        .ok_or_else(|| Error::Unavailable("topic_store 未注入".into()))?;
    store
        .mark_read(topic_id)
        .await
        .map_err(|e| Error::Internal(format!("topic mark_read: {e}")))?;
    Ok(StatusCode::NO_CONTENT)
}
```

文件头端点清单加 `POST /api/topics/:id/read`；顺手把第 8 行
`切玄女当前 topic（kill+spawn）` 改成 `切玄女当前 topic（常驻分身秒切，Phase 2）`，
`switch_topic` handler 的 doc（120-123 行）同步改：「Phase 2：ensure 常驻分身 +
set_current_topic，热路径毫秒级；懒启动冷路径数秒」。

`router.rs` 在 topics 路由组追加：

```rust
        .route(
            "/api/topics/{id}/read",
            post(handlers::topics::mark_topic_read),
        )
```

- [ ] **Step 4: 跑绿 + Commit**

```bash
cargo test -p fuxi-im topics
cargo fmt --all --check && cargo clippy -p fuxi-im --all-targets -- -D warnings
git add crates/fuxi-im/src/handlers/topics.rs crates/fuxi-im/src/router.rs
git commit -m "feat(im): /api/topics 带 unread_count + POST :id/read 水位端点"
```

### Task C3: web 前端 badge

- [ ] **Step 1: 类型 + client**

`web/src/types/api.ts` `TopicView` 加：

```ts
  /** Phase 2 · 未读数（非 user 消息、水位之后）。current topic 恒 0。 */
  unread_count?: number;
```

`web/src/lib/api.ts`：接口声明区（148-157 行附近）加注释行 +
client 实现区（330-348 行附近，照 `archiveTopic` 的 fetch 写法）加：

```ts
    markTopicRead: async (id: string): Promise<void> => {
      const res = await fetch(`/api/topics/${encodeURIComponent(id)}/read`, {
        method: "POST",
        credentials: "include",
      });
      if (!res.ok) throw new ApiError(res.status, await res.text());
    },
```

（ApiError 构造方式照本文件 archiveTopic 现状抄——若它走别的错误包装就保持一致。）

`web/tests/mocks/api.ts` 的 mock client 补 `markTopicRead: async () => {}`，
mock TopicView fixture 补 `unread_count: 0`。

- [ ] **Step 2: TopicSidebar badge + 标读**

`handleSwitch` 内 switch 成功后（`setCurrentTopicId(resp.current_topic_id)` 之后）：

```ts
      // Phase 2 · 未读水位：进入新话题立即标读；离开的旧话题也标读（看过的不算未读）。
      // fire-and-forget——badge 是辅助信息，失败不挡切换。
      void client.markTopicRead(id).catch(() => {});
      if (prev && prev !== id) void client.markTopicRead(prev).catch(() => {});
```

（`const prev = currentTopicId();` 放 `handleSwitch` 开头取。）

`TopicRow` 标题后插 badge（`rowTitle` span 之后、switching spinner 之前）：

```tsx
      <Show when={!props.current && (props.topic.unread_count ?? 0) > 0}>
        <span
          class={styles.rowBadge}
          data-testid={`topic-unread-${props.topic.id}`}
          aria-label={`${props.topic.unread_count} 条未读`}
        >
          {(props.topic.unread_count ?? 0) > 99 ? "99+" : props.topic.unread_count}
        </span>
      </Show>
```

`TopicSidebar.module.css` 加 `.rowBadge`——**复用文件里现有的 accent CSS 变量**
（先读该 css 文件，用 `.rowDot` / current 高亮同款变量；禁 emoji，纯色小药丸）：

```css
.rowBadge {
  flex: none;
  min-width: 18px;
  padding: 1px 6px;
  border-radius: 999px;
  font-size: 11px;
  font-weight: 600;
  line-height: 16px;
  text-align: center;
  /* 颜色变量以文件现状为准，例： */
  background: var(--accent, #e8590c);
  color: #fff;
}
```

- [ ] **Step 3: overlay 文案**（`XuannvPage.tsx:470` 附近）

`玄女正在重新接班` → `正在唤醒该话题的分身…`（热路径秒返几乎不可见，
冷路径懒启动时才停留）。

- [ ] **Step 4: 验证 + Commit**

```bash
cd crates/fuxi-im/web && npm run typecheck && npm test && npm run build
git add crates/fuxi-im/web
git commit -m "feat(pwa): 话题侧栏未读小红点 + 切换标读 + overlay 文案改分身语义"
```

---

## 块D · LRU cap 真执行（fuxi-orchestrator）

**Files:**
- Modify: `crates/fuxi-orchestrator/src/xuannv_pool.rs`（victim 排除 general + 返 id）
- Modify: `crates/fuxi-orchestrator/src/fuxi.rs`（FuxiConfig.xuannv_max_active +
  set_xuannv_for_topic 出口 enforce）

### Task D1: lru_victim_if_over_cap 改造

- [ ] **Step 1: 写失败测试**（`xuannv_pool.rs` tests 改 + 增）

现有 `lru_victim_none_when_under_cap` / `lru_victim_picks_least_recently_active_over_cap`
的断言改 tuple：`Some(t2)` → `assert_eq!(pool.lru_victim_if_over_cap().await.map(|(t, _)| t), Some(t2));`
新增：

```rust
    #[tokio::test]
    async fn lru_victim_never_selects_general() {
        // general 永驻公理（9151f54）：即使 general 是最久未活跃，victim 也必须
        // 跳过它选下一个。
        let pool = XuannvPool::new(1);
        let general = TopicId::general();
        let t2 = TopicId(uuid::Uuid::from_u128(2));
        pool.set_active(general, AgentId::new()).await; // tick 1（最老）
        pool.set_active(t2, AgentId::new()).await; // tick 2 —— 超 cap=1

        let victim = pool.lru_victim_if_over_cap().await;
        assert_eq!(victim.map(|(t, _)| t), Some(t2), "general 豁免，victim 应是 t2");
    }

    #[tokio::test]
    async fn lru_victim_returns_agent_id_without_touching_lru() {
        // victim 返 (topic, agent)——调用方拿 id 直接 shutdown，不再 active_id
        // 反查（active_id 会 touch LRU，把 victim 救活成最新）。
        let pool = XuannvPool::new(0);
        let t = TopicId(uuid::Uuid::from_u128(7));
        let a = AgentId::new();
        pool.set_active(t, a).await;
        assert_eq!(pool.lru_victim_if_over_cap().await, Some((t, a)));
        // 连问两次仍是它——证明没 touch LRU
        assert_eq!(pool.lru_victim_if_over_cap().await, Some((t, a)));
    }
```

- [ ] **Step 2: 跑红**

```bash
cargo test -p fuxi-orchestrator xuannv_pool
```

- [ ] **Step 3: 实现**

```rust
    /// 超 `max_active` 时返回应被 LRU 回收的 (topic, 分身 id)（最久未活跃的活分身）。
    /// **general 永不入选**（永驻公理 9151f54——cap 对 general 等于少占一个名额）。
    /// 直接带回 agent id：调用方不要再 `active_id` 反查（那会 touch LRU 把 victim
    /// 救活）。调用方负责 dormant（进程回收 + [`XuannvPool::remove`]）。未超返回 None。
    pub async fn lru_victim_if_over_cap(&self) -> Option<(TopicId, AgentId)> {
        let inner = self.inner.lock().await;
        if inner.active.len() <= self.max_active {
            return None;
        }
        inner
            .active
            .iter()
            .filter(|(t, _)| **t != TopicId::general())
            .min_by_key(|(t, _)| inner.lru_tick.get(t).copied().unwrap_or(0))
            .map(|(t, a)| (*t, *a))
    }
```

- [ ] **Step 4: 跑绿 + Commit**

```bash
cargo test -p fuxi-orchestrator xuannv_pool
git add crates/fuxi-orchestrator/src/xuannv_pool.rs
git commit -m "feat(pool): LRU victim 排除 general + 返回 agent id 防 touch 救活"
```

### Task D2: set_xuannv_for_topic 出口 enforce

- [ ] **Step 1: 写失败测试**（fuxi.rs `xuannv_pool_integration_tests` mod 追加；
  复用现有 `NullAgent` / `make_fuxi`）

```rust
    async fn make_fuxi_with_cap(cap: usize) -> Arc<Fuxi> {
        let dir = tempfile::tempdir().unwrap();
        let ws = Arc::new(GitWorktreeWorkspace::with_default_base(
            dir.path().to_path_buf(),
        ));
        let bus = EventBus::with_memory_store().await.unwrap();
        std::mem::forget(dir);
        Arc::new(Fuxi::with_config(
            bus,
            ws,
            FuxiConfig {
                allocate_worktree: false,
                base_branch: "main".into(),
                xuannv_max_active: Some(cap),
            },
        ))
    }

    #[tokio::test]
    async fn cap_overflow_dormants_lru_idle_victim() {
        let fuxi = make_fuxi_with_cap(1).await;
        let t1 = TopicId::new();
        let t2 = TopicId::new();
        fuxi.set_xuannv_for_topic(t1, AgentId::new()).await;
        fuxi.set_xuannv_for_topic(t2, AgentId::new()).await;
        // cap=1：t1（最老、不在 shelf = 非 busy）应被 dormant 摘出池
        assert_eq!(fuxi.xuannv_id_for_topic(t1).await, None, "LRU victim 应被回收");
        assert!(fuxi.xuannv_id_for_topic(t2).await.is_some(), "新分身保留");
    }

    #[tokio::test]
    async fn cap_never_evicts_general() {
        let fuxi = make_fuxi_with_cap(1).await;
        let general = TopicId::general();
        fuxi.set_xuannv_for_topic(general, AgentId::new()).await;
        let t2 = TopicId::new();
        fuxi.set_xuannv_for_topic(t2, AgentId::new()).await;
        // 超 cap 但 general 豁免——victim 只能是 t2 自己（cap=1 + general 常驻的
        // 退化配置；默认 cap=3 不会这样，断言语义即可）
        assert!(
            fuxi.xuannv_id_for_topic(general).await.is_some(),
            "general 永不被 cap 回收"
        );
    }

    #[tokio::test]
    async fn cap_skips_busy_victim() {
        // 软上限：victim 正跑 turn（Busy）则本轮放过，等 idle_gc 收。
        let fuxi = make_fuxi_with_cap(1).await;
        let agent = NullAgent::new("xuannv");
        let id = fuxi.insert_agent(agent, None).await;
        let t1 = TopicId::new();
        fuxi.set_xuannv_for_topic(t1, id).await;
        fuxi.shelf
            .set_status(id, crate::registry::ShelfStatus::Busy)
            .await;
        let t2 = TopicId::new();
        fuxi.set_xuannv_for_topic(t2, AgentId::new()).await;
        assert!(fuxi.xuannv_id_for_topic(t1).await.is_some(), "busy victim 本轮放过");
        assert!(fuxi.xuannv_id_for_topic(t2).await.is_some());
    }
```

- [ ] **Step 2: 跑红**

```bash
cargo test -p fuxi-orchestrator xuannv_pool_integration
```
预期：编译错（FuxiConfig 无 xuannv_max_active 字段）。

- [ ] **Step 3: 实现**

`FuxiConfig` 加字段 + Default：

```rust
    /// 玄女分身并发上限覆盖——None 走 env `FUXI_XUANNV_MAX_ACTIVE` / 默认 3。
    /// 测试注小值用；生产一般不设。
    pub xuannv_max_active: Option<usize>,
```

`with_config` 里 max_active 计算改（注意 cfg 在构造 Self 前先读）：

```rust
        let max_active = cfg.xuannv_max_active.unwrap_or_else(|| {
            std::env::var("FUXI_XUANNV_MAX_ACTIVE")
                .ok()
                .and_then(|s| s.parse::<usize>().ok())
                .filter(|n| *n > 0)
                .unwrap_or(3)
        });
```

`set_xuannv_for_topic` 出口追加 enforce：

```rust
    pub async fn set_xuannv_for_topic(&self, topic: fuxi_core::TopicId, id: AgentId) {
        self.xuannv_pool.set_active(topic, id).await;
        if topic == fuxi_core::TopicId::general() {
            let _ = self.xuannv_id.send_replace(Some(id));
        }
        self.enforce_xuannv_cap().await;
    }

    /// 块D：池超 cap 时 dormant 最久未活跃的非 general 分身（spec §4.6）。
    ///
    /// - victim **busy**（正跑 turn）→ 本轮放过（软上限，不丢 turn，等 idle_gc 收）。
    /// - 先 `pool.remove` 摘映射再 shutdown——`shutdown_agent` 的分身豁免
    ///   （is_active_clone）才放行，同 idle_gc 顺序。
    /// - shutdown 失败 warn + return 防死循环（victim 不可关时下次 set 再试）。
    async fn enforce_xuannv_cap(&self) {
        while let Some((topic, victim)) = self.xuannv_pool.lru_victim_if_over_cap().await {
            if self.shelf.status_of(victim).await == Some(crate::registry::ShelfStatus::Busy) {
                debug!(%topic, agent = %victim, "分身池超限但 victim busy——本轮放过（软上限）");
                return;
            }
            info!(%topic, agent = %victim, "分身池超限 → dormant LRU victim");
            self.xuannv_pool.remove(topic).await;
            let mut meta = EventMeta::now();
            meta.agent = Some(victim);
            let _ = self.bus.publish(Event {
                meta,
                kind: EventKind::AgentShuttingDown {
                    reason: "xuannv_cap_lru".into(),
                },
            });
            if let Err(e) = self.shutdown_agent(victim, "xuannv_cap_lru".into()).await {
                warn!(agent = %victim, error = %e, "cap 回收 shutdown 失败（victim 可能不在 shelf）");
                return;
            }
        }
    }
```

注意：测试里 victim 是裸 `AgentId::new()`（不在 shelf）时 `shutdown_agent` 会
AgentNotFound——`pool.remove` 已先行，断言（池里没了）仍成立；实现里
warn + return 即可，**不要**把 remove 挪到 shutdown 之后。

`FuxiConfig { ... }` 的 struct-literal 构造点会因新字段编译错——
`cargo check --workspace` 找全（repl.rs / im.rs / 测试），统一补
`xuannv_max_active: None,`（或改 `..FuxiConfig::default()`）。

- [ ] **Step 4: 跑绿 + 门禁 + Commit**

```bash
cargo test -p fuxi-orchestrator
cargo check --workspace
cargo fmt --all --check && cargo clippy -p fuxi-orchestrator --all-targets -- -D warnings
git add crates/fuxi-orchestrator/src/fuxi.rs crates/fuxi-orchestrator/src/xuannv_pool.rs
git commit -m "feat(pool): FUXI_XUANNV_MAX_ACTIVE 真执行——入池后 LRU dormant（busy 放过/general 豁免）"
```

---

## 块E · 集成收口（team-lead 串行）

### Task E1: merge + 全量门禁

- [ ] 四个 worktree 子分支 merge 回 `feat/fuxi-xuannv-phase2-routing`
  （顺序 D → B → A → C，im.rs 两处改动不同 hunk 应自动合；冲突手解）。
- [ ] Cargo.lock 若坏：`rm Cargo.lock && cargo build`（CLAUDE.md 陷阱条目）。
- [ ] 全量门禁：

```bash
cargo fmt --all --check
cargo clippy --workspace --all-targets -- -D warnings
cargo test --workspace
cd crates/fuxi-im/web && npm run typecheck && npm test && npm run build
```

### Task E2: 跨块冒烟（本机）

- [ ] `cargo run -p fuxi-cli -- im start`（或本机现行启动方式）起本地 daemon，
  PWA 开两个 topic 验证：热切秒级、新 topic 懒启动有 overlay、
  切走后旧 topic 分身仍在（`fuxi status` / log 看池）。

### Task E3: merge main + 部署 home + 实测

- [ ] 按 CLAUDE.md Git 公理 3：`git checkout main && git merge --no-ff feat/fuxi-xuannv-phase2-routing && git push origin main`。
- [ ] 部署按 memory `reference_home_deploy`：rsync crates+Cargo.* → home cargo build
  → stop fuxi-im → **cp 两份 binary**（`~/.local/bin/fuxi` + `~/.cargo/bin/fuxi`）
  → start → journalctl 烟测。PWA dist 按既有 rsync dist 方式同步。
- [ ] home 实测清单（spec §6）：
  1. 手机 PWA 在两个已有 topic 间切——秒切、无「重新接班」长等。
  2. topic A 派门客活 → 切到 B 聊天 → A 完工：B 视图不插话；sidebar A 亮未读；
     push 到手机；切回 A 历史里有完工汇报（latent bug 修复实证）。
  3. `FUXI_XUANNV_MAX_ACTIVE=2` 临时 systemd 环境下开 3 个 topic：最老的被 dormant
     （journalctl 见 `xuannv_cap_lru`），general 永远在。
  4. 重启 fuxi-im 后 general 自启正常（503 判断没被改坏）。
- [ ] handoff 文档 `docs/handoff/v1-session22.md` + memory 更新；
  开 issue 记「context/handoff watcher 仍只盯 general」（spec §5）。
