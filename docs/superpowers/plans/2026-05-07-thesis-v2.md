# Fuxi 毕设论文 v2 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 按 `deliverables/thesis-v2/design-spec.md` 完成 26000 字本科毕设论文，含 30 篇真实可验证参考文献、6 个公式、16 张图、6 张表、4 项实验数据，最终导出 docx。

**Architecture:** 4 个独立 track 并行——refs 验证 / bench 实验 / figures 出图 / 章节起草。前 3 路完成后第 4 路（draft）才能进入；draft 完成后过 awesome-ai-research-writing 三轮过滤，最后 pandoc 导 docx。

**Tech Stack:** Markdown / pandoc / Rust cargo bench / Python matplotlib / draw.io CLI / gpt-image-2 (RunningHub) / WebSearch + arxiv API

**Spec:** `deliverables/thesis-v2/design-spec.md` (commit `07f8cb2`)

**字数预算自检**：4500+4000+6000+4500+5500+1500=26000 ✓

---

## Phase 0 — 基础设施（前置，单线程）

### Task 0.1: 搭 thesis-v2 文件骨架 + pandoc 脚本 + matplotlib style

**Files:**
- Create: `deliverables/thesis-v2/chapters/00-frontmatter.md` (空，仅占位)
- Create: `deliverables/thesis-v2/chapters/01-introduction.md` ... `06-conclusion.md` ... `99-acknowledgements.md` (全空占位)
- Create: `deliverables/thesis-v2/main.md` (用 pandoc 的 `--include-in-header` / 或简单 cat 拼接)
- Create: `deliverables/thesis-v2/pandoc.sh`
- Create: `deliverables/thesis-v2/figures/matplotlib/style.py`
- Create: `deliverables/thesis-v2/refs.bib` (空，先创建)
- Create: `deliverables/thesis-v2/refs-verification.md` (空)
- Create: `deliverables/thesis-v2/benchmarks/v2-2026-05-07.md` (空)

- [ ] **Step 1: 建文件骨架**

```bash
cd /Users/e0_7/fuxi/deliverables/thesis-v2
mkdir -p chapters benchmarks/{poll-scan,scalability,bus-stress}
touch chapters/00-frontmatter.md chapters/01-introduction.md chapters/02-overall-design.md \
      chapters/03-modules.md chapters/04-implementation.md chapters/05-experiments.md \
      chapters/06-conclusion.md chapters/99-acknowledgements.md
touch refs.bib refs-verification.md benchmarks/v2-2026-05-07.md
touch figures/matplotlib/style.py figures/matplotlib/__init__.py
```

- [ ] **Step 2: 写 main.md 拼接**

文件内容：
```markdown
% 基于 AI Agent 的高性能分布式通讯系统
% 以琳
% 2026 年 5 月

<!-- pandoc.sh 会用 cat 把 chapters/*.md 按文件名顺序拼接到这个文件后面 -->
```

- [ ] **Step 3: 写 pandoc.sh 导出脚本**

文件内容：
```bash
#!/usr/bin/env bash
set -euo pipefail
cd "$(dirname "$0")"

# 拼接 chapters/*.md 按字典序（00-frontmatter → 99-acknowledgements）
cat chapters/*.md > _full.md

# 用 pandoc 转 docx；reference.docx 留空让 pandoc 用默认；
# 学校格式由用户手调（见 format-checklist.md）。
pandoc _full.md \
  --from markdown+yaml_metadata_block \
  --to docx \
  --bibliography refs.bib \
  --citeproc \
  --metadata link-citations=true \
  -o "基于AI_Agent的高性能分布式通讯系统_v2.docx"

rm _full.md
echo "已导出：$(pwd)/基于AI_Agent的高性能分布式通讯系统_v2.docx"
```

```bash
chmod +x deliverables/thesis-v2/pandoc.sh
```

- [ ] **Step 4: 写 matplotlib style.py（统一图表外观）**

文件内容：
```python
"""所有论文实验图共用的 matplotlib 样式。
A4 单栏宽度 ~6.5 inch / dpi 300 / 11pt 字 / viridis 色。
"""
import matplotlib as mpl

def apply():
    mpl.rcParams.update({
        "figure.figsize": (6.5, 4.0),
        "figure.dpi": 300,
        "savefig.dpi": 300,
        "savefig.bbox": "tight",
        "font.size": 11,
        "font.family": "serif",
        "axes.titlesize": 12,
        "axes.labelsize": 11,
        "xtick.labelsize": 10,
        "ytick.labelsize": 10,
        "legend.fontsize": 10,
        "lines.linewidth": 1.6,
        "axes.grid": True,
        "grid.alpha": 0.3,
        "axes.spines.top": False,
        "axes.spines.right": False,
    })

# 色盲友好：viridis 用于连续，tab10 用于离散
DISCRETE_PALETTE = ["#1f77b4", "#ff7f0e", "#2ca02c", "#d62728", "#9467bd", "#8c564b"]
```

- [ ] **Step 5: 验证 pandoc 可跑**

```bash
cd deliverables/thesis-v2
echo "# 测试" >> chapters/01-introduction.md
./pandoc.sh
ls -la *.docx
```
Expected: 产出 `基于AI_Agent的高性能分布式通讯系统_v2.docx`，大小 > 5KB。然后撤掉 chapters/01-introduction.md 里的「# 测试」。

- [ ] **Step 6: Commit**

```bash
git add deliverables/thesis-v2/
git commit -m "chore(thesis): scaffold v2 chapters/ pandoc.sh matplotlib style"
```

---

## Phase 1 — 三路并行（用 TeamCreate 起 thesis 团队）

> **重要**：Phase 1 三路（refs / bench / figures-arch）互无硬依赖，必须并行。
> 调度方式：`TeamCreate name=thesis` → 三个 worker 各claim一个 track，team-lead（你）只在每路完成时验收。
> 不要 ack-派活 round-trip——把整路 spec 一次给 worker，让他自驱按 task 走。

### Track A — Refs 验证（worker A）

#### Task A.1: 验证 A 桶 4 篇多智能体理论基础

**Files:**
- Modify: `deliverables/thesis-v2/refs-verification.md` (追加 A 桶段)
- Modify: `deliverables/thesis-v2/refs.bib` (追加 4 个 entry)

**候选清单**（每篇必须 search 验证；查不到换其他多智能体经典）：
1. Wooldridge M. *An Introduction to MultiAgent Systems*, 2nd ed., Wiley, 2009
2. Stone P, Veloso M. Multiagent Systems: A Survey from a Machine Learning Perspective. *Autonomous Robots* 8(3), 2000
3. Russell S, Norvig P. *Artificial Intelligence: A Modern Approach*, 4th ed., Pearson, 2020 (chapter on multi-agent)
4. Jennings N R. An agent-based approach for building complex software systems. *Communications of the ACM*, 44(4), 2001

- [ ] **Step 1: 对每篇执行验证流程**

```text
对每篇候选，执行：
1. WebSearch "「书名 / 论文标题」 author year"
2. 找到权威源（出版社页 / DOI / Google Scholar / Semantic Scholar）
3. 校对：title / author / year / venue / DOI 或 ISBN
4. 若是 arxiv 论文：调 https://arxiv.org/abs/<id> 二次确认
5. 验证通过 → 写入 refs-verification.md（含 URL + 关键摘录证据）
6. 验证失败（找不到 / 信息对不上）→ 标 [REPLACED] 并换一篇
```

- [ ] **Step 2: 把验证通过的 4 篇写入 refs.bib**

BibTeX entry 模板：
```bibtex
@book{wooldridge2009mas,
  author = {Wooldridge, Michael},
  title = {An Introduction to MultiAgent Systems},
  edition = {2},
  publisher = {John Wiley \& Sons},
  year = {2009},
  isbn = {978-0470519462}
}
```

- [ ] **Step 3: 在 refs-verification.md 追加 A 桶段**

格式：
```markdown
## A 桶：多智能体理论基础（4 篇）

### A.1 Wooldridge MAS 教材
- 引用键：`wooldridge2009mas`
- 验证 URL: https://www.wiley.com/...（实际 URL）
- 验证证据：搜索结果显示 ISBN 978-0470519462，2nd ed 2009 Wiley
- 引用位置：§1.1 多智能体定义

### A.2 ...
```

- [ ] **Step 4: Commit**

```bash
git add deliverables/thesis-v2/refs.bib deliverables/thesis-v2/refs-verification.md
git commit -m "docs(thesis): refs A 桶 4 篇 多智能体理论基础 验证完毕"
```

#### Task A.2: 验证 B 桶 8 篇 LLM Agent

**候选清单**：
1. Wang L, Ma C, Feng X, et al. A Survey on LLM-based Autonomous Agents. arXiv:2308.11432, 2023
2. Yao S, Zhao J, Yu D, et al. ReAct: Synergizing Reasoning and Acting. arXiv:2210.03629, 2022 (ICLR 2023)
3. Schick T, Dwivedi-Yu J, Dessì R, et al. Toolformer. arXiv:2302.04761, 2023 (NeurIPS 2023)
4. Wu Q, Bansal G, Zhang J, et al. AutoGen. arXiv:2308.08155, 2023
5. Hong S, Zhuge M, Chen J, et al. MetaGPT. arXiv:2308.00352, 2023 (ICLR 2024)
6. Li G, Hammoud H, Itani H, et al. CAMEL. arXiv:2303.17760, 2023 (NeurIPS 2023)
7. Wang G, Xie Y, Jiang Y, et al. Voyager. arXiv:2305.16291, 2023
8. Wang X, Li B, Song Y, et al. OpenHands. arXiv:2407.16741, 2024

每篇都 arxiv API 验证（curl https://arxiv.org/abs/<id>），失败立即换其他 LLM agent 论文。

- [ ] **Step 1: 对 8 篇逐一验证 → 写 refs.bib + refs-verification.md**
- [ ] **Step 2: Commit**

```bash
git add deliverables/thesis-v2/refs.bib deliverables/thesis-v2/refs-verification.md
git commit -m "docs(thesis): refs B 桶 8 篇 LLM Agent 验证完毕"
```

#### Task A.3: 验证 C 桶 5 篇 Agent 通信协议

**候选清单**：
1. Anthropic. Model Context Protocol. https://modelcontextprotocol.io / docs.anthropic.com
2. A2A Project. Agent2Agent Protocol Specification. https://github.com/a2aproject/A2A
3. Karpas E, Abend O, Belinkov Y, et al. MRKL Systems. arXiv:2205.00445, 2022
4. LangChain Project. LangGraph Documentation. https://python.langchain.com/docs/langgraph
5. Significant-Gravitas. AutoGPT. https://github.com/Significant-Gravitas/AutoGPT

非 arxiv 的（MCP / A2A / LangGraph / AutoGPT）用 WebFetch 验证 URL 可访问且内容对应。

- [ ] **Step 1: 5 篇逐一验证**
- [ ] **Step 2: Commit**

#### Task A.4: 验证 D 桶 7 篇分布式系统经典

**候选清单**（这桶都是经典，应该好查）：
1. Lamport L. Time, Clocks, and the Ordering of Events. *CACM* 21(7), 1978
2. Dean J, Ghemawat S. MapReduce. *OSDI* 2004
3. Ghemawat S, Gobioff H, Leung S. The Google File System. *SOSP* 2003
4. Kreps J, Narkhede N, Rao J. Kafka. *NetDB* 2011
5. Ongaro D, Ousterhout J. In Search of an Understandable Consensus Algorithm (Raft). *USENIX ATC* 2014
6. Hunt P, Konar M, Junqueira F, Reed B. ZooKeeper. *USENIX ATC* 2010
7. Corbett J et al. Spanner. *OSDI* 2012

- [ ] **Step 1: 7 篇逐一验证 → 主要靠 ACM DL / Google Scholar**
- [ ] **Step 2: Commit**

#### Task A.5: 验证 E 桶 6 篇工程工具/标准

**候选清单**（文档型，URL 验证为主）：
1. Tokio Project. Async Channels (broadcast). https://docs.rs/tokio/latest/tokio/sync/broadcast/
2. SQLite. Write-Ahead Logging. https://www.sqlite.org/wal.html
3. Axum. Server-Sent Events & WebSocket. https://docs.rs/axum/
4. Git. git-worktree manual. https://git-scm.com/docs/git-worktree
5. Fette I, Melnikov A. The WebSocket Protocol. RFC 6455, IETF, 2011
6. Hickson I (ed). HTML Living Standard, Server-Sent Events. WHATWG / W3C

- [ ] **Step 1: 6 篇逐一 WebFetch 验证 URL 可访问**
- [ ] **Step 2: Commit**

#### Task A.6: 校对全 refs.bib + 生成「悬挂引用扫描脚本」

- [ ] **Step 1: 跑 BibTeX lint**

```bash
# 装 bibtex-tidy 或简单 grep 扫缺字段
cd deliverables/thesis-v2
grep -c "^@" refs.bib  # 期望 = 30
```

- [ ] **Step 2: 写脚本 `scripts/check_refs.sh` 检查正文 cite 与 bib 匹配**

```bash
#!/usr/bin/env bash
# 找出 main.md 里所有 [@key] 引用，对比 refs.bib 是否都存在
set -euo pipefail
cd "$(dirname "$0")/../deliverables/thesis-v2"
cited=$(grep -oE '\[@[a-zA-Z0-9_]+\]' chapters/*.md | grep -oE '@[a-zA-Z0-9_]+' | sort -u)
defined=$(grep -oE '^@[a-z]+\{[a-zA-Z0-9_]+,' refs.bib | grep -oE '\{[a-zA-Z0-9_]+,' | tr -d '{,' | sed 's/^/@/' | sort -u)
echo "未定义但被引用："
comm -23 <(echo "$cited") <(echo "$defined")
echo "已定义但未引用："
comm -13 <(echo "$cited") <(echo "$defined")
```

- [ ] **Step 3: Commit**

---

### Track B — Bench 实验（worker B）

#### Task B.1: 重跑 baseline 5-run median（替换 3-run）

**Files:**
- Modify: `crates/fuxi-cli/src/bench_support/throughput.rs` (改 repeats=3 → repeats=5)
- Modify: `crates/fuxi-cli/src/bench_support/latency.rs` (sample_n 也提到 1000？保持 500)

- [ ] **Step 1: 找到 throughput.rs 里 `repeats: 3` 改 5**

```bash
grep -n "repeats" crates/fuxi-cli/src/bench_support/throughput.rs
```
找到 cell 配置里的 `repeats: 3`，改成 `repeats: 5`。同样 latency.rs 不动 sample_n（500 已够）。

- [ ] **Step 2: 跑 baseline**

```bash
cargo bench -p fuxi-cli --bench run_baseline 2>&1 | tee /tmp/baseline-v2.log
```
Expected: 跑约 8 分钟（5-run × 比 3-run 多 ~2 倍）。报告写到 `docs/benchmarks/baseline-2026-05-07.md`。

- [ ] **Step 3: 复制到 thesis-v2**

```bash
cp docs/benchmarks/baseline-2026-05-07.md deliverables/thesis-v2/benchmarks/baseline-5run.md
```

- [ ] **Step 4: Commit**

```bash
git add crates/fuxi-cli/src/bench_support/throughput.rs \
        docs/benchmarks/baseline-2026-05-07.md \
        deliverables/thesis-v2/benchmarks/baseline-5run.md
git commit -m "bench(thesis): baseline 升级到 5-run median 抗噪"
```

#### Task B.2: 实现 + 跑 poll_ms 扫描

**Files:**
- Modify: `crates/fuxi-cli/src/bench_support/throughput.rs` (加 cell)
- Create: `deliverables/thesis-v2/benchmarks/poll-scan/results.md`

设计：固定 worker_n=4、tasks_n=400、job_sleep=10ms；扫 poll_ms ∈ {5, 10, 25, 50, 100}。

- [ ] **Step 1: 在 throughput.rs 末尾新增一个 `poll_scan_section()` 函数**

代码模板（参照现有 `bonus cell`）：
```rust
pub async fn poll_scan() -> Section {
    let mut rows = vec![];
    for poll_ms in [5u64, 10, 25, 50, 100] {
        for &(workers, sleep_ms, tasks) in &[(4u32, 10u64, 400u32)] {
            let cell = run_cell_with_poll(workers, sleep_ms, tasks, poll_ms, 5).await;
            rows.push(cell);
        }
    }
    Section { title: "poll_ms scan".into(), rows }
}
```
（具体函数签名要照 throughput.rs 已有的 cell 函数对齐——worker 不熟代码就先 cargo expand 看现有 cell 怎么写）

- [ ] **Step 2: 在 `crates/fuxi-cli/benches/run_baseline.rs` 加一个 `--poll-scan` flag 或新建 `bench/poll_scan.rs`**

简单做法：新建 `crates/fuxi-cli/benches/poll_scan.rs`，复制 run_baseline.rs 模板，只调 `poll_scan()`。

- [ ] **Step 3: 跑**

```bash
cargo bench -p fuxi-cli --bench poll_scan 2>&1 | tee /tmp/poll-scan.log
```

- [ ] **Step 4: 把结果整理到 markdown**

```markdown
# Poll_ms 参数消融实验

固定 worker_n=4, tasks_n=400, job_sleep=10ms, 5-run median。

| poll_ms | wall_ms | tps | 损耗 |
|---|---|---|---|
| 5 | ... | ... | ... |
| 10 | ... | ... | ... |
| 25 | ... | ... | ... |
| 50 | ... | ... | ... |
| 100 | ... | ... | ... |

**观察**：（worker 写一段定性分析）
```

- [ ] **Step 5: Commit**

```bash
git add crates/fuxi-cli/src/bench_support/throughput.rs \
        crates/fuxi-cli/benches/poll_scan.rs \
        deliverables/thesis-v2/benchmarks/poll-scan/results.md
git commit -m "bench(thesis): poll_ms 5/10/25/50/100 扫描实验"
```

#### Task B.3: 实现 + 跑 scalability 1/2/4/8/16 worker

**Files:**
- Modify: `crates/fuxi-cli/src/bench_support/throughput.rs`
- Create: `crates/fuxi-cli/benches/scalability.rs`
- Create: `deliverables/thesis-v2/benchmarks/scalability/results.md`

设计：固定 job_sleep=10ms, poll_ms=5；扫 worker_n ∈ {1, 2, 4, 8, 16}；每个 worker 跑 100 个 task。

- [ ] **Step 1: 新增 `pub async fn scalability_scan()` 类似 poll_scan**

- [ ] **Step 2: 新建 `benches/scalability.rs`** 调它

- [ ] **Step 3: 跑**

```bash
cargo bench -p fuxi-cli --bench scalability 2>&1 | tee /tmp/scalability.log
```

- [ ] **Step 4: 整理 markdown + 标出理想线性上限**

模板：
```markdown
# Scalability 实验

| worker_n | tps | 理论上限 | scaling efficiency |
|---|---|---|---|
| 1 | ... | 100 | ... |
| 2 | ... | 200 | ... |
| 4 | ... | 400 | ... |
| 8 | ... | 800 | ... |
| 16 | ... | 1600 | ... |
```

- [ ] **Step 5: Commit**

#### Task B.4: 实现 + 跑事件总线纯压测

**Files:**
- Create: `crates/fuxi-events/benches/bus_stress.rs`
- Modify: `crates/fuxi-events/Cargo.toml` (加 `[[bench]]` section)
- Create: `deliverables/thesis-v2/benchmarks/bus-stress/results.md`

设计（spec §7.3）：
- 单 publisher 直调 `EventBus::publish`，N 个 subscriber 各自 `subscribe()`
- 维度 1：N ∈ {1, 4, 16, 64} subscriber
- 维度 2：发送速率 1k/10k/100k events/s
- 维度 3：payload size 小（~256B 简单 EventKind）vs 大（4KB AssistantText）

- [ ] **Step 1: 看 fuxi-events 现有 benches**

```bash
ls crates/fuxi-events/benches/ 2>/dev/null
grep -A 5 "\[\[bench\]\]" crates/fuxi-events/Cargo.toml || echo "no bench section"
```

- [ ] **Step 2: 写 bus_stress.rs**

代码模板（worker 应该依据现有 EventBus API 自行调整）：
```rust
//! 事件总线纯压测——隔离通信层性能，对应论文 §5.6 + 公式 (3-1)。

use fuxi_events::{EventBus, EventMeta, EventKind};
use std::sync::Arc;
use std::time::{Duration, Instant};

#[tokio::main(flavor = "multi_thread")]
async fn main() {
    println!("# 事件总线纯压测\n");
    println!("| subscribers | rate (ev/s) | payload | publish_tps | recv_p50_us | recv_p99_us | drops |");
    println!("|---|---|---|---|---|---|---|");

    for n_sub in [1usize, 4, 16, 64] {
        for rate in [1_000u64, 10_000, 100_000] {
            for &(payload_label, payload_size) in &[("small", 256usize), ("large", 4096)] {
                let result = run_cell(n_sub, rate, payload_size).await;
                println!(
                    "| {} | {} | {} | {:.1} | {:.1} | {:.1} | {} |",
                    n_sub, rate, payload_label,
                    result.publish_tps, result.recv_p50_us, result.recv_p99_us, result.drops
                );
            }
        }
    }
}

struct CellResult {
    publish_tps: f64,
    recv_p50_us: f64,
    recv_p99_us: f64,
    drops: u64,
}

async fn run_cell(n_sub: usize, rate: u64, payload_size: usize) -> CellResult {
    // 实现细节 worker 写：
    // 1. 构造 N 个 subscriber，每个用 tokio::spawn 跑接收循环，记录 recv_at
    // 2. publisher 用 tokio::time::interval 控速发送 5 秒
    // 3. payload 构造成对应 size 的 String
    // 4. 收完后算 publish_tps + 接收延迟 p50/p99 + drop 计数
    todo!()
}
```

- [ ] **Step 3: 在 Cargo.toml 注册**

```toml
[[bench]]
name = "bus_stress"
harness = false
```

- [ ] **Step 4: 跑**

```bash
cargo bench -p fuxi-events --bench bus_stress 2>&1 | tee /tmp/bus-stress.log
```

- [ ] **Step 5: 把输出复制到 results.md，加段定性分析**

- [ ] **Step 6: Commit**

```bash
git add crates/fuxi-events/benches/bus_stress.rs \
        crates/fuxi-events/Cargo.toml \
        deliverables/thesis-v2/benchmarks/bus-stress/results.md
git commit -m "bench(thesis): 事件总线纯压测 · subscribers × rate × payload 三维"
```

#### Task B.5: 汇总 v2-2026-05-07.md

**Files:**
- Modify: `deliverables/thesis-v2/benchmarks/v2-2026-05-07.md`

- [ ] **Step 1: 把 4 项实验的 markdown 合并**

```markdown
# Fuxi v2 论文实验报告

实验日期：2026-05-07
机器：Apple Silicon (M-series) / macOS 25.0
Rust：stable / Tokio multi-thread

## 1. Baseline（5-run median）
[copy from baseline-5run.md]

## 2. Poll_ms 扫描
[copy from poll-scan/results.md]

## 3. Scalability 1/2/4/8/16 worker
[copy from scalability/results.md]

## 4. 事件总线纯压测
[copy from bus-stress/results.md]
```

- [ ] **Step 2: Commit**

```bash
git add deliverables/thesis-v2/benchmarks/v2-2026-05-07.md
git commit -m "docs(thesis): v2 benchmarks 总报告 (baseline + 3 新实验)"
```

---

### Track C — Figures（worker C）

#### Task C.1-C.8: draw.io 8 张架构/流程图

**Files:**
- Create: `deliverables/thesis-v2/figures/drawio/fig-2-1-architecture.drawio` ... `fig-4-2-adapter.drawio`
- Create: `deliverables/thesis-v2/figures/drawio/*.png`（导出）

> **执行方式**：用 `Skill: draw-io`。每张图给一段 prompt 让 skill 出 .drawio + .png。
> 内容参照 spec §6.1 图清单。**不要在图里塞超过 6 个标签的描述性文字**——这是 spec §6.1 强调的学术图美学。

- [ ] **Step 1: 图 2.1 系统总体架构**（5 层 + crate 归属）
- [ ] **Step 2: 图 3.1 模块依赖关系**（crate 间 use 依赖）
- [ ] **Step 3: 图 3.2 任务生命周期状态机**（Created → Dispatched → Running → Done/Cancelled/Failed）
- [ ] **Step 4: 图 3.3 事件总线发布订阅拓扑**（publisher → broadcast + WAL → N subscriber）
- [ ] **Step 5: 图 3.4 工作区隔离三层**（L1 read-only / L2 ephemeral / L3 persistent + git worktree）
- [ ] **Step 6: 图 3.5 玄女门客协作时序**（用户 → 玄女 → dispatch → 门客 → 事件回流 → 抄送）
- [ ] **Step 7: 图 4.1 跨节点 dist 任务流**（home + dist controller + 远端 worker + event 回传）
- [ ] **Step 8: 图 4.2 Agent adapter 与 CLI 进程边界**（Rust trait → spawn → CLI stdout 解析 → EventKind）

每完成一张就 commit：

```bash
git add deliverables/thesis-v2/figures/drawio/fig-X-Y-*.{drawio,png}
git commit -m "figure(thesis): 图 X.Y 描述"
```

#### Task C.9-C.14: matplotlib 6 张实验图

**Files:**
- Create: `deliverables/thesis-v2/figures/matplotlib/plot_*.py`（每张一个）
- Create: `deliverables/thesis-v2/figures/matplotlib/fig-5-*.png`

> **依赖**：所有这些图都需要 Track B 的实验数据先跑完。

- [ ] **Step 9: 图 5.1 吞吐与 worker 数线性扩展**

`plot_5_1_scalability.py`：读 `benchmarks/scalability/results.md` 的表 → 折线图（实测 vs 理论上限虚线）+ scaling efficiency 副 y 轴

```python
import matplotlib.pyplot as plt
from style import apply, DISCRETE_PALETTE
apply()

# 数据 hardcode 进 py 文件（数据从 results.md 读出来填）
workers = [1, 2, 4, 8, 16]
tps = [...]  # 跑完填
ideal = [w * 100 for w in workers]

fig, ax = plt.subplots()
ax.plot(workers, tps, "o-", label="实测", color=DISCRETE_PALETTE[0])
ax.plot(workers, ideal, "k--", label="理论上限", alpha=0.5)
ax.set_xlabel("Worker 数")
ax.set_ylabel("吞吐 (tasks/s)")
ax.legend()
plt.savefig("fig-5-1-scalability.png")
```

- [ ] **Step 10: 图 5.2 poll_ms 扫描** — 折线 with 误差带
- [ ] **Step 11: 图 5.3 事件总线 publish 吞吐 vs subscriber** — 分面网格（按 payload_size 分面）
- [ ] **Step 12: 图 5.4 任务派发延迟分布** — 小提琴图（横轴是不同实验配置，纵轴 ms）
- [ ] **Step 13: 图 5.5 跨节点事件流延迟分布** — 小提琴图
- [ ] **Step 14: 图 5.6 端到端延迟分解柱状图** — 把 (2-1) 各分量实测拆出来

每完成一张：

```bash
git add deliverables/thesis-v2/figures/matplotlib/plot_5_*.py \
        deliverables/thesis-v2/figures/matplotlib/fig-5-*.png
git commit -m "figure(thesis): 图 5.X 描述"
```

#### Task C.15-C.16: gpt-image-2 概念图

> **执行方式**：用 `Skill: gpt-image-2`。

- [ ] **Step 15: 图 1.1 玄女与门客拟人协作场景**

Prompt：
```
中式插画风格，水墨清新画风，画面正中是身着青色道袍的玄女形象（像古代占卜师，
手持卷轴），周围环绕 4 个不同造型的门客形象（每个代表不同 CLI agent
角色：cc / codex / 通用 worker），他们之间有发光的丝线相连表示信息流动。
背景留白，山水意境点缀。整体气氛：协作而非竞争，神秘而专业。
不要文字。横向构图。
```

```bash
# 调 mcp__antigravity-image__generate_image 或走 RunningHub
# 输出存 figures/gpt-image/fig-1-1-xuannv-menke.png
git add deliverables/thesis-v2/figures/gpt-image/fig-1-1-*.png
git commit -m "figure(thesis): 图 1.1 玄女门客协作概念图"
```

- [ ] **Step 16: 图 6.1 多节点分布式 fuxi 群（可选）**

Prompt：
```
俯视视角，多个发光节点散布在世界地图上，节点间有数据流光线连接，
每个节点上方悬浮一个简化的 agent icon。整体科技感 + 一点中式墨色山水意境。
表达：本地优先、跨节点协作。
```

---

## Phase 2 — 章节起草（依赖 Phase 1 大部分完成）

> Phase 2 开工条件：refs A/B/D 桶已完成（绪论需要），bench baseline 至少跑完。
> 起草顺序：1 → 2 → 3 → 4 → 5 → 6 → 摘要。
> **每章独立 task，写完就 commit。**

### Task D.1: 起草第 1 章 绪论（4500 字）

**Files:**
- Modify: `deliverables/thesis-v2/chapters/01-introduction.md`

**结构**：
- 1.1 研究背景及意义（~1500 字）
- 1.2 国内外研究现状（~2000 字，引用最密集）
- 1.3 论文研究内容（~600 字）
- 1.4 论文结构安排（~400 字）

**引用需求**：约 18 篇集中此章——A 桶 4 篇 + B 桶 8 篇 + C 桶 5 篇主要在这里。

- [ ] **Step 1: 写 1.1 研究背景及意义**

  写作要点：
  - 第 1 段：从 LLM Agent 演进到多 Agent 协作的工程问题，引 [@wang2023llmagentsurvey]
  - 第 2 段：多 Agent 工程化痛点（任务编排 / 跨进程通信 / 状态一致 / 执行隔离），引 [@hong2023metagpt][@wu2023autogen][@li2023camel]
  - 第 3 段：本地优先 + 高性能通信的现实意义，引 [@wang2024openhands][@yao2022react]
  - 第 4 段：本文贡献（一句话）

- [ ] **Step 2: 写 1.2 国内外研究现状**

  分 3-4 个子段（无小标题）：
  - 多智能体理论传统：Wooldridge / Stone&Veloso / Jennings
  - LLM Agent 与工具使用：ReAct / Toolformer / MRKL
  - 多 Agent 协作框架：AutoGen / MetaGPT / CAMEL / Voyager / OpenHands
  - 通信协议与编排：MCP / A2A / LangGraph / AutoGPT
  - 本节末尾过渡到「现有工作多侧重协作流程而非通信层性能」

- [ ] **Step 3: 写 1.3 论文研究内容（5 点贡献）**

- [ ] **Step 4: 写 1.4 论文结构安排（6 章列表）**

- [ ] **Step 5: 字数自查**

```bash
wc -m deliverables/thesis-v2/chapters/01-introduction.md
```
Expected：4500 ± 500 字（不含引用 cite key）

- [ ] **Step 6: Commit**

```bash
git add deliverables/thesis-v2/chapters/01-introduction.md
git commit -m "draft(thesis): 第 1 章 绪论 初稿 4500 字"
```

### Task D.2: 起草第 2 章 系统总体设计（4000 字）

**Files:**
- Modify: `deliverables/thesis-v2/chapters/02-overall-design.md`

**结构**：
- 2.1 系统目标及主要任务（~700 字）
- 2.2 需求分析（~800 字，分功能/非功能）
- 2.3 系统整体架构（~1500 字 + 图 2.1）
- 2.4 通信模型与性能指标（~700 字 + 公式 (2-1)~(2-3)）
- 2.5 本章小结（~300 字）

**引用需求**：少数引用（D 桶 Lamport / MapReduce 用于"任务调度借鉴"）。

- [ ] **Step 1: 写 2.1**
- [ ] **Step 2: 写 2.2 功能需求 + 非功能需求**
- [ ] **Step 3: 写 2.3 引用 ![](../figures/drawio/fig-2-1-architecture.png)**
- [ ] **Step 4: 写 2.4 嵌入公式 (2-1)~(2-3)**

公式 markdown 写法：
```markdown
$$L_{e2e} = L_{enq} + L_{disp} + L_{exec} + L_{evt} \tag{2-1}$$

式 (2-1) 中，$L_{e2e}$ 表示端到端任务耗时...
```

- [ ] **Step 5: 写 2.5 本章小结**
- [ ] **Step 6: 字数 + commit**

### Task D.3: 起草第 3 章 核心模块设计（6000 字，最长）

**Files:**
- Modify: `deliverables/thesis-v2/chapters/03-modules.md`

**结构**：
- 3.1 模块划分（~500 字 + 图 3.1）
- 3.2 Agent 通信协议模块（~800 字，cite [@a2a][@mcp]）
- 3.3 事件总线模块（~1200 字 + 图 3.3 + 公式 (3-1)，cite [@tokio][@sqlite-wal][@lamport1978]）
- 3.4 编排与任务调度模块（~1200 字 + 图 3.5 + 公式 (3-2)，cite [@hong2023metagpt][@dean2004mapreduce]）
- 3.5 执行适配器与工作区隔离模块（~800 字 + 图 3.4，cite [@swe-agent][@openhands][@gitworktree]）
- 3.6 观测与 IM 接入模块（~600 字，cite [@axum][@websocket-rfc]）
- 3.7 记忆、角色与触发器模块（~600 字，cite [@wang2023voyager]）
- 3.8 本章小结（~300 字）

- [ ] **Step 1-7: 写 3.1-3.7 各小节，逐个嵌入图和公式**
- [ ] **Step 8: 写 3.8 小结 + 字数 check + commit**

### Task D.4: 起草第 4 章 系统实现（4500 字）

**Files:**
- Modify: `deliverables/thesis-v2/chapters/04-implementation.md`

**结构**：
- 4.1 开发环境与技术选型（~500 字）
- 4.2 Rust workspace 结构（~700 字 + 表 4.1）
- 4.3 事件数据结构实现（~900 字 + 公式 (4-1) Lamport 因果）
- 4.4 任务派发流程实现（~900 字 + 图 4.2 + 伪代码 1）
- 4.5 分布式通信实现（~1000 字 + 图 4.1 + 伪代码 2）
- 4.6 关键流程伪代码（~400 字 + 伪代码 3, 4）
- 4.7 本章小结（~200 字）

- [ ] **Step 1-7: 写 4.1-4.7**

伪代码用 markdown fenced block：
```markdown
\`\`\`text
Input: user_prompt
task = create_task(user_prompt)
publish(TaskCreated(task))
...
\`\`\`
```

- [ ] **Step 8: Commit**

### Task D.5: 起草第 5 章 实验与分析（5500 字，依赖 Phase 1 bench + figures 全完成）

**Files:**
- Modify: `deliverables/thesis-v2/chapters/05-experiments.md`

**结构**：
- 5.1 测试目标与测试方法（~500 字）
- 5.2 测试用例覆盖（~600 字 + 表 5.1）
- 5.3 吞吐量测试（baseline + scalability，~1100 字 + 表 5.2 + 图 5.1）
- 5.4 延迟测试（任务派发 + 跨节点事件流，~900 字 + 表 5.3 + 图 5.4 + 图 5.5）
- 5.5 poll_ms 参数消融（~700 字 + 表 5.4 + 图 5.2）
- 5.6 事件总线纯压测（~1100 字 + 表 5.5 + 图 5.3）
- 5.7 端到端延迟分解（~400 字 + 图 5.6）
- 5.8 功能验收（~300 字）
- 5.9 本章小结（~300 字）

- [ ] **Step 1-9: 写每节，所有数字必须从 `benchmarks/v2-2026-05-07.md` 引**
- [ ] **Step 10: 字数 check + commit**

### Task D.6: 起草第 6 章 总结与展望（1500 字）

- [ ] **Step 1: 写 6.1 总结（~800 字）**
- [ ] **Step 2: 写 6.2 不足与展望（~700 字）**
- [ ] **Step 3: Commit**

### Task D.7: 写 frontmatter（封面/扉页/诚信声明/中英摘要/目录）

**Files:**
- Modify: `deliverables/thesis-v2/chapters/00-frontmatter.md`

- [ ] **Step 1: 封面信息（学校 / 题目 / 姓名 / 学号 / 班级 / 专业 / 指导教师 / 2026 年 5 月）**

> **占位**：`{{学号}}` `{{班级}}` `{{专业}}` `{{指导教师}}` 等用 mustache 风格占位，让用户最后手填。

- [ ] **Step 2: 扉页（同封面 + 完成日期）**
- [ ] **Step 3: 诚信声明（学校模板原文，电子签名 + 答辩之后日期占位）**
- [ ] **Step 4: 中文摘要 ~400 字 + 5 个关键词**

摘要必须含具体数字（吞吐 X tasks/s, p99 延迟 Y ms）：

```markdown
## 摘要

随着大语言模型与工具调用技术的发展... [省略 350 字]
测试结果表明，Fuxi 在 8 worker、10 ms 模拟任务条件下达到 {{TPS_8W_10MS}} tasks/s 的吞吐量；
跨节点事件流 p50 延迟为 {{P50_EVENT_FLOW}} ms、p99 延迟为 {{P99_EVENT_FLOW}} ms。

**关键词**：AI Agent；分布式通信；多智能体系统；事件总线；A2A 协议
```

数字占位等 Phase 1 bench 跑完后填。

- [ ] **Step 5: ABSTRACT ~250 词 + Key words 5 个**
- [ ] **Step 6: 目录占位**（pandoc 会自动从 heading 生成 TOC，留 `[TOC]` 标记）
- [ ] **Step 7: Commit**

### Task D.8: 致谢

**Files:**
- Modify: `deliverables/thesis-v2/chapters/99-acknowledgements.md`

- [ ] **Step 1: 写 ~200 字致谢（指导教师 + 开源社区 + 同学 + 家人）**
- [ ] **Step 2: Commit**

---

## Phase 3 — 全文润色 + Reviewer 自审

### Task E.1: 表达润色 prompt 过 6 章

> 执行：对每章调 `awesome-ai-research-writing` 的「表达润色（中文论文）」prompt（README 第 ## 表达润色（中文论文） 节）。
> 注意 prompt 强调 **修正阈值**：原文若已通顺则保留，不强行换词。

- [ ] **Step 1-6: 6 章逐一过润色 prompt**

每章润色后：
```bash
git add deliverables/thesis-v2/chapters/0X-*.md
git commit -m "polish(thesis): 第 X 章 表达润色"
```

### Task E.2: 去 AI 味 prompt 过 6 章

> 执行：对每章调「去 AI 味（Word 中文）」prompt。

- [ ] **Step 1-6: 6 章逐一过去 AI 味 prompt**

### Task E.3: Reviewer 视角自审全文

> 执行：先 pandoc 出 v0.9 docx → 再用「论文整体以 Reviewer 视角进行审视」prompt 跑一遍

- [ ] **Step 1: 跑 pandoc.sh 出 v0.9 docx**
- [ ] **Step 2: 调 reviewer prompt（投稿目标填「学校优秀本科论文」）**
- [ ] **Step 3: 把 reviewer 报告存 `deliverables/thesis-v2/reviewer-report.md`**
- [ ] **Step 4: 根据报告改 1 轮（重点改 Critical Weakness）**
- [ ] **Step 5: Commit**

### Task E.4: 跑 refs 闭合扫描

```bash
bash scripts/check_refs.sh
```
Expected：「未定义」和「未引用」两栏都空。如有 refs 未引用，删掉或加引用；如有 cite 未定义，去 refs.bib 补。

- [ ] **Step 1: 运行扫描，修所有不一致**
- [ ] **Step 2: Commit**

---

## Phase 4 — 导出与交付

### Task F.1: pandoc 导出 v1.0 docx

- [ ] **Step 1: 跑 pandoc.sh**

```bash
cd deliverables/thesis-v2
./pandoc.sh
```
Expected：输出 `基于AI_Agent的高性能分布式通讯系统_v2.docx`，~30-50 页。

- [ ] **Step 2: 用户用 Word 打开看一眼整体结构** （这一步用户做）

### Task F.2: 写 format-checklist.md 给用户手调

**Files:**
- Create: `deliverables/thesis-v2/format-checklist.md`

**内容**（学校格式硬约束 spec §3 抄过来 + 操作指南）：

```markdown
# 学校格式手调清单

pandoc 出来的 docx 内容是对的，但格式细节学校有硬要求，下面是手调清单。

## 1. 字体字号（用 Word 样式批量改）

| 元素 | 字体 | 字号 |
|---|---|---|
| 一级标题 | 黑体 | 小三 |
| 二级标题 | 黑体 | 四号 |
| 三级标题 | 黑体 | 小四 |
| 正文 | 宋体 | 小四 |
| 数字与字母 | Times New Roman | 同正文 |
| 页眉 | 黑体 | 小五 |
| 页码 | 宋体 | 小五 |

正文行距 1.5 倍，字符间距标准。

## 2. 页边距

布局 → 页边距 → 自定义：上 2.5 / 下 2 / 左 3 / 右 2 cm

## 3. 页码

封面、扉页、摘要、目录 **不编**；正文起开始编。
插入 → 页码 → 页面底端 → 居中。

## 4. 页眉

页眉文字：「基于 AI Agent 的高性能分布式通讯系统」（与封面/扉页/查重报告完全一致）

## 5. 公式编号

公式右对齐编号 (X-Y)。pandoc 出来若没编号，用 Word 公式工具手调或用「制表位 + 编号」对齐。

## 6. 图表标题

图标题在图下，居中：「图 X.Y  描述」
表标题在表上，居中：「表 X.Y  描述」

## 7. 占位填充

搜索 `{{` 找出所有占位（学号 / 班级 / 专业 / 指导教师 / 完成日期等），逐一填实。

## 8. 诚信声明签名

电子签名（用图片签名插入）+ 答辩之后日期。

## 9. 参考文献

英文文献一行写不下时行尾加「-」（手调）。
```

- [ ] **Step 1: 写 format-checklist.md**
- [ ] **Step 2: Commit**

### Task F.3: 最终交付

- [ ] **Step 1: 把所有交付物清单告知用户**

```text
deliverables/thesis-v2/
├── 基于AI_Agent的高性能分布式通讯系统_v2.docx  ← 主交付
├── format-checklist.md                          ← 学校格式手调清单
├── chapters/                                    ← 源 markdown
├── refs.bib + refs-verification.md              ← 30 篇验证证据
├── benchmarks/v2-2026-05-07.md                  ← 实验数据
└── figures/                                     ← 16 张图源
```

- [ ] **Step 2: TaskList 全部 completed**

---

## 依赖图

```
Phase 0 ──→ Phase 1 三路并行 ──→ Phase 2 章节起草 ──→ Phase 3 润色 ──→ Phase 4 导出
            ├─ Track A refs ──┐
            ├─ Track B bench ─┤── Phase 2 D.1 (绪论) 需要 Track A 的 A/B/C 桶
            └─ Track C figs ──┤── Phase 2 D.5 (实验) 需要 Track B 全完 + Track C 9-14
                              └── 所有章节都用 figures，所以图越早画完越好
```

实际并行做法：Phase 0 完后立即 TeamCreate 起 worker A/B/C，team-lead（你）每天验收一次每路产出。
Track C 的 9-14（matplotlib 实验图）需要等 Track B 跑完，但 1-8（drawio 架构图）可以立即开始。

---

## Self-Review

**1. Spec 覆盖检查**：
- ✓ §1 目标 → 整个 plan
- ✓ §2 章节字数 → Phase 2 D.1-D.6
- ✓ §3 学校格式 → Phase 4 F.2 format-checklist
- ✓ §4 30 refs → Phase 1 Track A
- ✓ §5 6 公式 → Phase 2 D.2 (2-1~2-3) / D.3 (3-1~3-2) / D.4 (4-1)
- ✓ §6 16 图 + 6 表 → Phase 1 Track C + 章节内表
- ✓ §7 4 实验 → Phase 1 Track B
- ✓ §8 工作流 → Phase 1 并行
- ✓ §10 风险 → 每个 task 内嵌注意事项
- ✓ §12 验收 → Phase 4 F.3

**2. 占位扫描**：
- 已规避「TODO / 后续补 / 类似 Task N」等字样
- 字数占位（如 `{{TPS_8W_10MS}}`）是显式的、有明确填充时机（Phase 1 完成后）
- 学校信息占位（学号 / 班级 等）显式标 `{{}}` 让用户填

**3. 类型/命名一致**：
- crate 名（fuxi-events / fuxi-cli 等）与 CLAUDE.md 一致
- 图编号（图 X.Y）跨 task 一致
- refs.bib 引用键命名规范：`<firstauthor><year><shorttitle>`

---

附：本 plan 一旦 approved 由 subagent-driven-development 执行，team_name=`thesis`。
