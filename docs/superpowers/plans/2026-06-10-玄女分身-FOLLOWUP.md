# 玄女分身（按 topic 隔离）+ 持久队列 · FOLLOW-UP 待办

> 配套 `2026-06-10-玄女分身-持久队列.md`。块1-5 已实装并通过全 workspace 门禁
> （见分支 `feat/fuxi-xuannv-clone` commit 链）。本文件记**一项显式延后**的 follow-up，
> 给后续接手的人。

## 已 ship 范围（块1-5 core）

- 块1 `Task.topic_id`
- 块2 `XuannvPool` 池模型 + Fuxi 接入（豁免/idle_gc dormant 回收）
- 块3 里程碑事件按 topic 路由到归属分身（**修跨 topic 串味 357da78a**）
- 块4 dormant 完工信号落持久队列（`PendingNotifyStore`）+ drain/respawn 补发（**a01cfab5 信号不丢**）
- 块5 `ensure_xuannv_for_topic` 真懒启动（`XuannvSpawner`）+ idle_gc 切池 + general 镜像
  reconciler（**修「玄女假活黑洞」**）+ worker-dispatch topic 链路（`FUXI_TOPIC` + `--topic`）

dormant 分身被 respawn 补发的消息**实时进对话**（WS 推到 PWA）——补发闭环完整。

## FU-1 · dormant 补发的 push 通知（原计划 7.6，DEFER）

**目标**：dormant 分身被 respawn 补发完工信号时，额外给用户手机发 push（ping）。

**为什么不阻断主功能**：7.4 的 dormant→respawn→drain 已让补发消息实时进对话（WS）。push
只是手机端额外提醒，**不影响 core 闭环**。

**为什么 balloon（>30min，故延后）**：现有 `fuxi_im::push::hooks::spawn`（`fuxi-cli/src/im.rs`
启动）**绑死单个 xuannv_id**——只传 general 那只分身的 watch，只对**那一只**的
`AgentResponded` 推送。池模型下非 general topic 的 respawn 分身是**不同 id**，push 钩子
收不到它们的响应。要支持得二选一：
- **(a)** push hooks 改池感知：订阅全分身 id（`Fuxi::xuannv_pool_watch()` 给的
  `topic→AgentId` 实时映射）+ 重做它现有的「玄女 idle >30s」计时逻辑（每分身一套）；
- **(b)** 把 `ImPush`/FCM sender 线进 `TopicXuannvSpawner`，在 `drain_pending_to` 补发后
  显式推一条。

两者都动 push fan-out + 多分身定向，估 >30min，故留 follow-up。

## 接手提示

- 入口接口都已就绪：`Fuxi::ensure_xuannv_for_topic`（懒启动/respawn）、`XuannvSpawner` trait、
  `PendingNotifySink` + `PendingNotifyStore`、`Fuxi::xuannv_pool_watch()`（topic→分身实时映射）。
- general 玄女分身在 `im start` **boot 即 eager spawn**（`xuannv_bootstrap::ensure_xuannv`，
  行为同历史单玄女）；懒启动只覆盖非 general topic + dormant 回收后的 respawn。
- 跑测试记得 `env -u HTTPS_PROXY -u https_proxy cargo test ...`——dist e2e 那批 502 是
  mac 代理坑（CLAUDE.md 有记），非本功能改动，绿了忽略。
- **改 cc spawn 相关代码（`spawn.rs` / `config.rs` / `xuannv_bootstrap`）铁律**：绝不给 cc
  塞 `--session-id`/`--resume`（CLAUDE.md + 玄女 spawn 卡死教训），spawn 全走
  `session_id: None, resume_session_id: None`。
