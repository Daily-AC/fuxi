% 图 2-2 端到端数据流 v2 — 修：编号气泡放箭头起点旁，文字标签居中，不互相压
\documentclass[border=4pt]{standalone}
\usepackage{xeCJK}
\setCJKmainfont{Songti SC}
\setCJKsansfont{Heiti SC}
\usepackage{tikz}
\usepackage{amsmath}
\usetikzlibrary{arrows.meta, positioning, calc, shapes.geometric, fit, backgrounds}
\input{_style.tex}

\begin{document}
\begin{tikzpicture}[font=\sffamily, x=24mm, y=-9mm]

% ── 6 条 lifelines（横向）──
\foreach \i/\name/\fill in {%
  0/{用户}/white,
  1/{玄女}/{fuxiBlue!8},
  2/{Shelf}/white,
  3/{门客 cc}/white,
  4/{EventBus}/{fuxiBlue!8},
  5/{SQLite/IM}/white%
} {
  \node[modBox, anchor=south, fill=\fill, minimum width=20mm] (h\i) at (\i, 0) {\name};
  \draw[fuxiGrayLine!60, thick] (\i, -0.4) -- (\i, -10);
}

% ── 时序消息：编号气泡放在源 lifeline 紧贴右侧（不压标签）──
% step 1: 用户 → 玄女
\node[numBubble, fill=fuxiBlue!8] at (0.15, -1) (n1) {1};
\draw[flow] (0, -1) -- node[above=1pt, font=\scriptsize] {用户输入} (1, -1);

% step 2: 玄女 → Shelf
\node[numBubble, fill=fuxiBlue!8] at (1.15, -1.8) {2};
\draw[flow] (1, -1.8) -- node[above=1pt, font=\scriptsize] {claim\_idle} (2, -1.8);

% step 3: 玄女 → 门客（A2A dispatch）
\node[numBubble, fill=fuxiBlue!8] at (1.15, -2.6) {3};
\draw[flow, color=fuxiBlue] (1, -2.6) -- node[above=1pt, font=\scriptsize, color=fuxiBlue] {A2A dispatch} (3, -2.6);

% 门客内部 fork（小标签）
\node[ghostBox, anchor=west, minimum width=18mm, font=\scriptsize\itshape] (cli) at (3.1, -3.6) {fork cc CLI 子进程};

% step 4: 门客 → EventBus
\node[numBubble, fill=fuxiBlue!8] at (3.15, -4.8) {4};
\draw[flowEvent] (3, -4.8) -- node[above=1pt, font=\scriptsize, color=fuxiBlue] {event publish} (4, -4.8);

% step 5: EventBus → SQLite (WAL)
\node[numBubble, fill=fuxiBlue!8] at (4.15, -5.6) {5};
\draw[flowEvent] (4, -5.6) -- node[above=1pt, font=\scriptsize, color=fuxiBlue] {WAL append} (5, -5.6);

% step 5': EventBus → IM 广播（点线，与 5 同步）
\draw[flowDup] (4, -6.4) -- node[above=1pt, font=\scriptsize, color=fuxiBlue!70] {broadcast $\rightarrow$ IM} (5, -6.4);

% step 6: 门客 → EventBus (result + done)
\node[numBubble, fill=fuxiBlue!8] at (3.15, -7.4) {6};
\draw[flowEvent] (3, -7.4) -- node[above=1pt, font=\scriptsize, color=fuxiBlue] {result + done} (4, -7.4);

% step 7: EventBus → 玄女（订阅）→ 用户（stream-back）
\node[numBubble, fill=fuxiBlue!8] at (4.15, -8.4) {7};
\draw[flowDup] (4, -8.4) -- (1.05, -8.4);
\draw[flow] (1, -9) -- node[above=1pt, font=\scriptsize] {stream-back} (0, -9);

% 时间轴
\draw[->, color=fuxiGrayDark, thick] (-0.7, -0.8) -- (-0.7, -10)
  node[midway, rotate=90, above, font=\scriptsize\itshape, color=fuxiGrayDark] {时间};

\end{tikzpicture}
\end{document}
