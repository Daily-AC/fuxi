% 图 3-1 模块依赖 DAG v3 — 显式坐标 5 层网格，避免相对定位让 cli 与 cc 同 y
\documentclass[border=4pt]{standalone}
\usepackage{xeCJK}
\setCJKmainfont{Songti SC}
\setCJKsansfont{Heiti SC}
\usepackage{tikz}
\usepackage{amsmath}
\usetikzlibrary{arrows.meta, positioning, calc, shapes.geometric, fit, backgrounds}
\input{_style.tex}

\begin{document}
\begin{tikzpicture}[font=\sffamily,
    every node/.style={transform shape},
    x=10mm, y=10mm]

% ── 5 层显式 y 坐标 ────────────────────────────
% L1 y=0, L2 y=2.2, L3 y=4.4, L4 y=6.6, L5 y=8.8

% L1：核心
\node[modBox, minimum width=24mm] (core) at (0, 0) {fuxi-core\\\scriptsize trait + 基础类型};

% L2：基础设施 3 个
\node[modBox, minimum width=24mm] (events) at (-3.5, 2.2) {fuxi-events\\\scriptsize EventBus + WAL};
\node[modBox, minimum width=24mm] (memory) at (0, 2.2) {fuxi-memory\\\scriptsize 抽取/召回};
\node[modBox, minimum width=24mm] (a2a) at (3.5, 2.2) {fuxi-a2a\\\scriptsize A2A v1.0};

% L3：编排 4 个
\node[highlightBox, minimum width=26mm] (orch) at (-3.5, 4.4) {fuxi-orchestrator\\\scriptsize Fuxi + Shelf};
\node[modBox, minimum width=24mm] (firehose) at (0, 4.4) {fuxi-firehose\\\scriptsize WS hub + TUI};
\node[modBox, minimum width=24mm] (im) at (3.5, 4.4) {fuxi-im\\\scriptsize PWA 后端};
\node[modBox, minimum width=24mm] (sched) at (1.75, 6.0) {fuxi-scheduler\\\scriptsize cron};

% L4：适配器 2 个
\node[modBox, minimum width=24mm] (cc) at (-2, 6.6) {fuxi-agent-cc};
\node[modBox, minimum width=24mm] (codex) at (-4.7, 6.6) {fuxi-agent-codex};

% L5：顶层 1 个
\node[highlightBox, minimum width=26mm] (cli) at (0, 8.4) {fuxi-cli\\\scriptsize daemon + binaries};

% ── 依赖箭头 ──────────────────────────────────
\draw[flow] (events) -- (core);
\draw[flow] (a2a) -- (core);
\draw[flow] (memory) -- (core);
\draw[flow] (memory) -- (events);

\draw[flow] (orch) -- (events);
\draw[flow] (orch) -- (core);
\draw[flow] (orch) -- (a2a);
\draw[flow] (sched) -- (events);
\draw[flow] (sched) -- (orch);
\draw[flow] (firehose) -- (events);
\draw[flow] (im) -- (events);
\draw[flow] (im) -- (orch);

\draw[flow] (cc) -- (a2a);
\draw[flow] (cc) -- (orch);
\draw[flow] (codex) -- (a2a);
\draw[flow] (codex) -- (orch);

\draw[flow] (cli) -- (orch);
\draw[flow] (cli) -- (firehose);
\draw[flow] (cli) -- (im);
\draw[flow] (cli) -- (sched);
\draw[flow] (cli) -- (memory);
\draw[flow] (cli) -- (cc);
\draw[flow] (cli) -- (codex);

% ── 层带标注（统一锚到 events.west 左 14mm 的固定 x，每层 y 取该层 box）──
\coordinate (legendCol) at ($(events.west)+(-14mm,0)$);
\foreach \layer/\rowAnchor in {%
  L1 核心/core,
  L2 基础设施/events,
  L3 编排/orch,
  L4 适配器/cc,
  L5 顶层/cli%
} {
  \node[anchor=east, font=\scriptsize\itshape, text=fuxiGrayDark]
    at (legendCol |- \rowAnchor) {\layer};
}

% ── 反公理注释（下移到 fuxi-core 下方独立行）──
\node[anchor=north, font=\scriptsize\itshape, text=fuxiOrange, align=center]
  at ($(core.south)+(0,-8mm)$) {%
    反公理：\texttt{fuxi-memory}\,$\not\to$\,\texttt{fuxi-orchestrator}（adapter 实现放在 \texttt{fuxi-cli} 顶层）
  };

\end{tikzpicture}
\end{document}
