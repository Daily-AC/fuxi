# fuxi-im · v2 PWA Design Context

## Design Context

### Users

- **唯一用户**：以琳（fuxi 主理人）。单用户，无多租户。
- **场景**：白天 ERP 上班，从手机派活给 home fuxi；通勤/碎片看进度；插话回玄女。
- **核心 job**：跟玄女对话主导 — task / 节点是次要 surface。
- **设备**：iPhone 主力（PWA 添加到主屏），偶尔 Android。
- **认知负担**：用户对 fuxi 内部架构充分了解，UI 不必解释术语。

### Brand Personality

- **三词**：克制、技术感、自家味儿（不像别的 IM）。
- **声音**：直白，不寒暄不卖萌。错误说人话不掉书袋。
- **情绪目标**：信任 + 控制感 — "这是我自己的工具，安静干活"，不是 "AI 助手取悦我"。

### Aesthetic Direction（v2 重定调）

**减法是核心**：v1 失败在"功能堆砌 + 输入框在顶 + 玄女空荡 + emoji 残留"。v2 是大道至简的 IM：主屏 = 对话，副视图 sheet 召唤。

- **暗底 first**：`#0a0a0a`，不是纯黑。
- **正文 sans-serif**：PingFang SC / 系统字栈，15px 正文、16px 标题、18px 大标题、13px 次要、11px meta。等宽**仅限** code / 工具输出 / agent id。
- **平面卡片**：圆角（card 12 / bubble 14 / pill 22）但**纯平**——没有 shadow、gradient、glassmorphism、伪 3D。靠间距 + 1px `#262626` 细边切分。
- **触控热区 ≥ 44px**。
- **流式动效**：fuxi cyan pulse dot（不是字符 cursor）。除此之外 transition ≤ 200ms 无弹跳，`prefers-reduced-motion` 全关。
- **不用 emoji**。任何"图标需求"用 6px css 圆点 / 1 行小字代替。
- **不用 Unicode block 装饰**（▎┌─└→……是 TUI 语言，PWA 禁）。
- **anti-references**：WeChat / Slack / iMessage / Telegram 的视觉签名一律避开。Material shadow、glassmorphism、紫蓝 AI 助手霓虹光全禁。

### Design Principles

1. **对话是主语**：主屏永远是 chat。任务 / 节点是 sheet 召唤。
2. **小屏密度优于装饰**：靠 typography 层级 + 间距 + 1px 细边切结构，不靠 effect。
3. **Anthropic 橙 pulse dot 是唯一花活**：除流式 pulse 外无非必要动画。
4. **等宽出现 = 这块内容真的需要等宽**：agent id、code、tool 输出、diff——其余一律中文 sans。
5. **触控优先**：每个交互元素 44px 起；hover 状态可以有但不依赖。
6. **不为视觉牺牲信息**：状态色用低饱和、色盲下可区分，必要时配文字标签。
7. **role tint 用于身份**：玄女紫 / 鲁班金 / 蒲松绿。User bubble 用 `userBubble` 棕。

### Concrete Tokens（v2，源头同步在 src/tokens.ts + src/styles/global.css）

| token | value | 用途 |
|---|---|---|
| `--bg` | `#1F1E1B` | app 底色 · 暖灰棕 |
| `--surface` | `#2A2925` | 卡片底 / bubble 底 |
| `--surface-elevated` | `#33312D` | task card / 抬高层 |
| `--border` | `#3A3835` | 1px 细边 |
| `--border-strong` | `#48453F` | hover / 抬高 |
| `--text-primary` | `#F5F1E8` | 正文 · 奶白 |
| `--text-secondary` | `#B8B0A0` | 次要 · 米白 |
| `--text-muted` | `#807868` | meta / placeholder |
| `--accent` | `#D97757` | Anthropic 橙 · focus / pulse / send 按钮 |
| `--accent-subtle` | `#3F2E26` | accent 低亮态 / hover 暗底 |
| `--accent-dim` | `#3F2E26` | 兼容字段 |
| `--on-accent` | `#1A0F08` | accent 底上的文字色，深棕（≥ 4.5:1 对比） |
| `--xuannv` | `#C4A8E8` | 玄女 role 紫 |
| `--luban` | `#E5A547` | 鲁班 role 金 |
| `--pusong` | `#A0C277` | 蒲松 role 绿 |
| `--user-bubble` | `#3D332A` | 用户消息底 · 棕调 |
| `--user-bubble-text` | `#F5F1E8` | 用户消息正文 |
| `--success / warning / danger` | `#88B47B / #E5B047 / #D97777` | 状态色（暖系协调） |
| `--radius-card` | `12px` | 卡片 / 任务卡 |
| `--radius-bubble` | `14px` | message bubble |
| `--radius-pill` | `22px` | 输入框 / send / drag handle |
| `--touch` | `44px` | 最小热区 |
| `--header-h` | `52px` | 顶栏高度 |
| `--composer-h` | `64px` | 底部输入条 |
| `--font-sans` | system + PingFang SC + Noto Sans CJK | 正文 |
| `--font-mono` | JetBrains Mono / SF Mono | code / agent id |
| `--size-meta/aux/body/heading/title` | `11/13/15/16/18px` | 字号阶梯 |
| `--weight-normal/medium/semibold` | `400/500/600` | 字重 |
